---
phase: 09.4-indexer-hardening
plan: 01
subsystem: testing
tags: [rust, tdd, indexer, walkdir, path-normalization]

# Dependency graph
requires:
  - phase: 09.3-asset-protocol-security-hardening
    provides: stable indexer module to append tests to
provides:
  - three RED test stubs in indexer.rs locking contracts for path normalization and max-depth guards
affects:
  - 09.4-02 (Plan 02 turns these RED stubs GREEN by implementing normalize_for_exclusion and WalkDir max_depth)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD Wave 0: append #[test] stubs that FAIL against current code; turn GREEN in the next wave"
    - "Deviation discovery: plan's RED premise for trailing-slash-only test was wrong (Path::starts_with handles it); revised test combines uppercase + trailing separator to expose the actual case-sensitivity bug"

key-files:
  created: []
  modified:
    - src-tauri/src/indexer.rs

key-decisions:
  - "test_crawl_excludes_trailing_slash revised to combine uppercase directory name + trailing separator; the uppercase component exposes the genuine case-sensitivity bug while the trailing separator documents that normalization must handle both issues together"
  - "Three RED tests verify two distinct bugs: (1) case-sensitive Path::starts_with on user-supplied excluded paths; (2) unbounded WalkDir depth that traverses 9-level deep directories"

patterns-established:
  - "Wave 0 TDD in indexer: append stubs after last existing test, no production code changes, compile+run to verify RED"

requirements-completed: []

# Metrics
duration: 7min
completed: 2026-03-11
---

# Phase 09.4 Plan 01: Wave 0 RED Test Stubs Summary

**Three failing test stubs in indexer.rs locking contracts for case-insensitive path exclusion and WalkDir max-depth (items 9 and 10) — 56 pre-existing tests remain GREEN**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-11T20:42:15Z
- **Completed:** 2026-03-11T20:49:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Appended three `#[test]` functions to the `mod tests` block in `src-tauri/src/indexer.rs`
- `test_crawl_excludes_normalized`: exposes case-sensitive `Path::starts_with` — uppercase excluded path does not exclude lowercase filesystem entry (apps.len() == 2, asserts 1)
- `test_crawl_excludes_trailing_slash`: exposes case-sensitivity failure with combined uppercase + trailing backslash excluded path (same root bug, documents that normalize_for_exclusion must handle both)
- `test_crawl_respects_max_depth`: exposes unbounded WalkDir — directory at depth 9 is discovered when it should not be (asserts deep.exe absent, but it is present)
- 56 pre-existing tests pass, 3 new stubs fail, 2 existing ignored — exact Wave 0 target state

## Task Commits

Each task was committed atomically:

1. **Task 1: Write three RED test stubs in indexer.rs tests module** - `0e422aa` (test)

**Plan metadata:** (to be committed with this SUMMARY)

## Files Created/Modified
- `src-tauri/src/indexer.rs` - Three new `#[test]` functions appended to `mod tests` block (lines 938-1017)

## Decisions Made
- Revised `test_crawl_excludes_trailing_slash` test: plan's original version (forward-slash only) was GREEN against current code because `Path::starts_with` normalizes separators on Windows. Changed to uppercase-directory + trailing-backslash combination so the test is genuinely RED due to the case-sensitivity bug. This keeps the test name accurate (trailing slash is part of the combination) while ensuring it exercises a real current failure.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Revised test_crawl_excludes_trailing_slash to achieve genuine RED state**
- **Found during:** Task 1 (test authoring)
- **Issue:** Plan specified forward-slash + trailing separator as the excluded path. On Windows, `Path::starts_with` accepts both forward and backward slashes and strips trailing separators, so the test passed GREEN instead of RED.
- **Fix:** Changed excluded path to uppercase directory component + trailing backslash. The uppercase subdirectory name (`EXCLUDED`) does not match the lowercase actual directory (`excluded`) under Rust's case-sensitive `Path::starts_with`, making the test genuinely RED. The trailing backslash is preserved to document that normalization must strip it.
- **Files modified:** src-tauri/src/indexer.rs
- **Verification:** `cargo test test_crawl_excludes_trailing_slash` reports FAILED with `left: 2, right: 1` assertion error
- **Committed in:** 0e422aa (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — incorrect RED premise in plan)
**Impact on plan:** Required revision; the test now exposes a genuine bug and will turn GREEN in Plan 02 when `normalize_for_exclusion()` handles both case and trailing separators. No scope creep.

## Issues Encountered
- Plan's analysis of `Path::starts_with` trailing-slash behavior was incorrect for Windows: the Rust stdlib Path type normalizes separator characters at component level, so neither trailing backslash nor forward slashes break `starts_with`. The case-sensitivity issue (uppercase vs lowercase directory names) is the genuine bug.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Wave 0 complete: three RED stubs established
- Plan 02 (Wave 1) implements `normalize_for_exclusion()` and `WalkDir::max_depth(8)` to turn these RED stubs GREEN
- No blockers

---
*Phase: 09.4-indexer-hardening*
*Completed: 2026-03-11*
