---
phase: 07-context-menu
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified: []
autonomous: false
requirements:
  - MENU-01
  - MENU-02
  - MENU-03

must_haves:
  truths:
    - "Right-click on launcher background shows menu at cursor position"
    - "Right-click on a result row shows no menu"
    - "Escape while menu open dismisses menu only, launcher stays"
    - "Escape with menu closed hides launcher"
    - "Click-outside-menu closes menu"
    - "Quit Launcher exits the process"
  artifacts:
    - path: "src/App.vue"
      provides: "verified context menu overlay"
      contains: "menuVisible"
    - path: "src-tauri/src/commands.rs"
      provides: "verified quit_app command"
      contains: "pub fn quit_app"
  key_links:
    - from: "human right-click gesture"
      to: "menu overlay visible at cursor"
      via: "onContextMenu + menuVisible + template v-if"
      pattern: "MENU-01 end-to-end"
---

<objective>
Human verification that the complete context menu feature works correctly in the running Tauri application.

Purpose: Confirm all three MENU requirements work end-to-end in the actual running app, since menu behavior (positioning, dismissal, keyboard interaction) cannot be fully verified by build tools alone.
Output: Confirmed working context menu or issue report for gap closure.
</objective>

<execution_context>
@C:/Users/Balazs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-context-menu/07-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Context menu overlay in the launcher window (Phase 7):
    - Right-click on launcher background opens a custom dark-themed overlay positioned at cursor
    - Two items: "Settings" and "Quit Launcher"
    - Backdrop closes menu on click-outside
    - Escape key: first press closes menu, second press hides launcher
    - quit_app Rust command exits the process cleanly
    - Menu state resets when launcher is hidden/reshown via hotkey
  </what-built>
  <how-to-verify>
    Run the app: `pnpm tauri dev` from the riftle project root.

    Once the launcher is visible (press Alt+Space if needed):

    1. MENU-01 — Right-click on the launcher background (not on a result row):
       Expected: A dark overlay menu appears at the cursor position, showing "Settings" and "Quit Launcher". No native browser context menu.

    2. MENU-01 — Right-click on a result row (type something to get results first):
       Expected: Nothing happens. No menu appears. No native context menu.

    3. MENU-03 — With menu open, click anywhere on the launcher outside the menu:
       Expected: Menu closes. Launcher stays visible.

    4. MENU-03 — Open menu again. Press Escape:
       Expected: Menu closes. Launcher stays visible. Search input gets focus (cursor is in the input field).

    5. MENU-03 — With menu closed, press Escape:
       Expected: Launcher hides (existing behavior unchanged).

    6. MENU-02 — Open menu again. Click "Settings":
       Expected: Menu closes. A console.error may appear (open_settings_window not yet implemented in Phase 8) — this is expected and correct. Launcher remains open.

    7. MENU-02 — Open menu again. Click "Quit Launcher":
       Expected: The launcher process exits cleanly. The window closes.

    8. Start the app again. Open menu via right-click. Press Alt+Space (or let the window hide some other way). Press Alt+Space to show again:
       Expected: Menu is NOT visible when launcher reappears. Menu state was reset on hide.

    Visual checks:
    - Menu has the same dark gradient background as the launcher
    - Menu has the same 1px rgba(255,255,255,0.15) border
    - Menu has the same 9px border-radius
    - Hover state on menu items is the same blue highlight (rgba(10,132,255,0.18)) as selected result rows
    - No separator line between "Settings" and "Quit Launcher"
    - Menu items are text-only (no icons)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All MENU-01, MENU-02, MENU-03 requirements verified end-to-end in the running application.
</verification>

<success_criteria>
- Right-click opens overlay at cursor on background (MENU-01)
- Right-click on result row does nothing (MENU-01)
- Settings item closes menu and silently fails invoke (MENU-02, Phase 8 wires real behavior)
- Quit Launcher item exits the process (MENU-02)
- Escape closes menu first, then hides launcher on second press (MENU-03)
- Click-outside closes menu (MENU-03)
- Menu state resets on hide/show cycle (MENU-03)
- Menu visual style consistent with launcher aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/07-context-menu/07-02-SUMMARY.md` using the summary template.
</output>
