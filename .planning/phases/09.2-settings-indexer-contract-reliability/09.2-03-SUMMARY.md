---
phase: 09.2-settings-indexer-contract-reliability
plan: 03
subsystem: backend
tags: [rust, store, tauri, settings, timer, mpsc]

# Dependency graph
requires:
  - phase: 09.2-02
    provides: TimerMsg enum in indexer.rs and Arc<Mutex<mpsc::Sender<TimerMsg>>> managed state in lib.rs
provides:
  - get_settings_cmd JSON now includes system_tool_allowlist field (prevents silent allowlist reset on frontend round-trip)
  - set_settings_cmd notifies background timer thread of new reindex_interval via TimerMsg::SetInterval
affects: [frontend Settings.vue round-trips, background indexer timer, 09.2-04]

# Tech tracking
tech-stack:
  added: []
  patterns: [tauri::Manager trait imported locally in function body to access try_state without widening file import footprint]

key-files:
  created: []
  modified:
    - src-tauri/src/store.rs

key-decisions:
  - "use tauri::Manager imported locally inside set_settings_cmd body — avoids widening the import footprint of store.rs while enabling try_state access"
  - "try_state used instead of State parameter for timer sender — safe in non-desktop builds and tests where timer state may not be managed"

patterns-established:
  - "Local use-in-function-body pattern for trait imports: bring tauri::Manager into scope only where needed rather than at module level"

requirements-completed: [SIC-04, SIC-05]

# Metrics
duration: 5min
completed: 2026-03-10
---

# Phase 09.2 Plan 03: store.rs contract fixes — allowlist in get_settings_cmd and timer wiring in set_settings_cmd

**Fixed two store.rs bugs: system_tool_allowlist added to get_settings_cmd JSON response and set_settings_cmd now notifies the running timer thread of interval changes via TimerMsg::SetInterval using try_state**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-10T20:29:47Z
- **Completed:** 2026-03-10T20:31:53Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Added `"system_tool_allowlist": settings.system_tool_allowlist` to the `serde_json::json!({})` block in `get_settings_cmd`, preventing silent overwrite when the frontend round-trips settings through save
- Wired `set_settings_cmd` to send `TimerMsg::SetInterval(settings.reindex_interval)` to the background timer thread after persisting settings, so interval changes take effect without restart
- All 6 Phase 09.2 contract tests GREEN; 43 total tests pass, 0 failures

## Task Commits

Each task was committed atomically:

1. **Task 1 + Task 2: Fix get_settings_cmd allowlist omission and wire timer interval** - `79344ab` (fix)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `src-tauri/src/store.rs` - Added allowlist field to get_settings_cmd JSON; wired timer notification in set_settings_cmd via try_state + TimerMsg::SetInterval

## Decisions Made
- `use tauri::Manager` imported locally inside `set_settings_cmd` function body to access `try_state` without widening the module-level import footprint — consistent with plan's stated approach of local `use` inside function
- `try_state` used instead of `tauri::State<T>` parameter — avoids runtime panic in non-desktop builds where the timer state may not be managed

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added missing `use tauri::Manager` import for try_state**
- **Found during:** Task 1 (cargo build verification)
- **Issue:** `try_state` is provided by the `tauri::Manager` trait, which was not imported in store.rs. The plan showed the pattern from search.rs which imports `tauri::Manager` at module level, but didn't explicitly mention adding it to store.rs imports
- **Fix:** Added `use tauri::Manager;` as a local use statement inside `set_settings_cmd` function body (consistent with the plan's recommendation to use local `use` to avoid widening import footprint)
- **Files modified:** src-tauri/src/store.rs
- **Verification:** `cargo build` produced zero errors after fix; all 43 tests pass
- **Committed in:** 79344ab (task commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - missing import needed for trait method access)
**Impact on plan:** Required for correctness — try_state is a trait method on Manager. No scope creep.

## Issues Encountered
- First build attempt failed with `no method named try_state found for struct AppHandle<R>` — resolved by adding `use tauri::Manager` locally inside the function body

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Both store.rs bugs are fixed; all 6 Phase 09.2 tests GREEN
- Phase 09.2 plans 01-03 are complete; plan 04 (if any) or phase close can proceed
- Settings round-trips now preserve the system_tool_allowlist; timer responds immediately to interval changes from Settings UI

---
*Phase: 09.2-settings-indexer-contract-reliability*
*Completed: 2026-03-10*
