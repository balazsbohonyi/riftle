---
phase: 09.9
slug: show-launcher-on-default-monitor
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-06-14
---

# Phase 09.9 — Validation Strategy (Gap Closure)

> Per-phase validation contract for gap closure plans (documentation fixes only).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust unit tests) + `pnpm build` (frontend) |
| **Config file** | `src-tauri/Cargo.toml` (test deps: tempfile) |
| **Quick run command** | `cargo test --lib` |
| **Full suite command** | `cargo test && pnpm build` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `grep` validation targets
- **After plan completion:** Run `cargo test && pnpm build` (existing code must not regress)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 09.9-03-01 | 03 | 1 | MON-01..05 | smoke | `grep -c "MON-0[1-5]" .planning/REQUIREMENTS.md` | ✅ | ⬜ pending |
| 09.9-03-02 | 03 | 1 | ROADMAP | smoke | `grep -c "09.9-02" .planning/ROADMAP.md` | ✅ | ⬜ pending |
| 09.9-03-03 | 03 | 1 | regression | unit | `cargo test && pnpm build` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all gap closure requirements. No Wave 0 needed — code is already verified (101 tests pass, 8/8 truths, `pnpm build` passes).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Settings window always centers on primary monitor | MON-02 | Window positioning requires visual verification | Open settings, close, re-open; verify centered on primary each time |
| Cursor-position monitor detection on multi-monitor | MON-04 | Requires multi-monitor hardware | Enable toggle, move cursor to secondary monitor, summon launcher |
| Mixed-DPI multi-monitor positioning | MON-04 | Requires mixed-DPI hardware | Test with 4K @ 200% + 1080p @ 100% |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
