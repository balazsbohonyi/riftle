---
status: resolved
phase: 09.5-backend-resilience
source:
  - 09.5-01-SUMMARY.md
  - 09.5-02-SUMMARY.md
  - 09.5-03-SUMMARY.md
  - 09.5-04-SUMMARY.md
started: 2026-03-12T00:35:00Z
updated: 2026-03-12T01:41:08Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Fully quit Riftle, then start it again from scratch. The launcher should boot without an error dialog or crash, appear when summoned, and show a usable search UI with live results.
result: pass

### 2. Backend Warning Banner Delivery
expected: When a backend recovery warning is present, the launcher shows a visible inline warning banner on open. Dismissing it removes the banner cleanly without leaving broken spacing or blocking search interaction.
result: issue
reported: "The warning displays the path to the backup file twice. The second copy uses a JetBrains-style monospace font and gray color; keep just one backup path and make it lighter, almost white."
severity: cosmetic

### 3. Settings Recovery From Malformed settings.json
expected: If `settings.json` is malformed before launch, Riftle should still start, create or overwrite `settings.json.bak`, restore usable defaults, and show a warning that settings were recovered instead of failing silently or crashing.
result: pass

### 4. Database Recovery From Corrupt launcher.db
expected: If `launcher.db` is corrupt before launch, Riftle should still start, create or overwrite `launcher.db.bak`, show a warning that launch history was reset, and keep the launcher usable for search.
result: pass

### 5. Post-Recovery Search And Launch
expected: After any recovery warning path, the launcher should still let you search for an app and launch it successfully, without the UI freezing or the launcher failing to hide/continue normally.
result: pass

## Summary

total: 5
passed: 4
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "When a backend recovery warning is present, the launcher shows a visible inline warning banner on open with clear, non-duplicated backup-path text."
  status: resolved
  reason: "User reported: The warning displays the path to the backup file twice. The second copy uses a JetBrains-style monospace font and gray color; keep just one backup path and make it lighter, almost white."
  severity: cosmetic
  test: 2
  root_cause: "Backend recovery warnings duplicate the backup path in both `message` and `backup_path`, and the launcher renders both fields. The second rendering uses the monospace `warning-path` style with a dim gray token."
  artifacts:
    - path: "src-tauri/src/lib.rs"
      issue: "DB recovery warning includes the backup path inside `message` and also sets `backup_path`."
    - path: "src-tauri/src/store.rs"
      issue: "Settings recovery warning includes the backup path inside `message` and also sets `backup_path`."
    - path: "src/App.vue"
      issue: "Banner template renders `warning.message` and `warning.backup_path` separately."
    - path: "src/styles/tokens.css"
      issue: "`warning-path` styling inherits JetBrains Mono and a dim text color token."
  missing:
    - "Choose a single source of truth for the backup path in recovery warnings."
    - "Render the backup path only once in the launcher warning banner."
    - "Restyle the displayed backup path to a lighter, near-white color."
  debug_session: ".planning/debug/phase-09-5-warning-path-dup.md"
