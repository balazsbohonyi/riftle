---
phase: 09.5-backend-resilience
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/src/warnings.rs
  - src/App.vue
autonomous: true
requirements:
  - "none (urgent internal hardening, no formal req IDs)"
must_haves:
  truths:
    - "A managed pending-warning queue exists so startup warnings are not lost before Vue mounts"
    - "Rust exposes a `take_backend_warnings` command and emits a `backend-warning` event for later warnings"
    - "The launcher shows a minimal dismissible warning banner without introducing a broader notification system"
    - "Warning infrastructure is generic enough for both DB reset and settings recovery flows"
  artifacts:
    - path: "src-tauri/src/warnings.rs"
      provides: "BackendWarning payload, queue state, emit/enqueue helpers"
      contains: "BackendWarning"
    - path: "src-tauri/src/lib.rs"
      provides: "managed warning queue and `take_backend_warnings` invoke registration"
      contains: "take_backend_warnings"
    - path: "src/App.vue"
      provides: "warning banner render, startup drain call, event listener"
      contains: "backend-warning"
  key_links:
    - from: "Rust warning producers"
      to: "launcher UI after mount"
      via: "enqueue into pending queue and emit `backend-warning`"
      pattern: "backend-warning"
    - from: "App.vue startup hook"
      to: "take_backend_warnings command"
      via: "drains queued startup warnings before listening for live events"
      pattern: "take_backend_warnings"
---

<objective>
Build the minimum warning delivery infrastructure needed for Phase 09.5: a backend warning payload type, a pending-warning queue for startup safety, a drain command, a live event, and a small launcher banner in `App.vue`.

Purpose: Give the later DB/settings recovery plans a reliable way to surface destructive-recovery warnings without adding a broader notification subsystem.
Output: `warnings.rs` plus targeted `lib.rs` and `App.vue` integration.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/09.5-backend-resilience/09.5-RESEARCH.md
@.planning/phases/09.5-backend-resilience/09.5-VALIDATION.md
@src-tauri/src/lib.rs
@src/App.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared backend warning model and pending-warning queue</name>
  <files>src-tauri/src/warnings.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Introduce a small serializable `BackendWarning` payload with stable fields for `kind`, `title`, `message`, and `backup_path`
    - Add a managed queue state that stores warnings produced before the frontend listener exists
    - Add helper functions that both enqueue a warning and best-effort emit `backend-warning`
    - Expose a `take_backend_warnings` Tauri command that drains the queue atomically
  </behavior>
  <action>
    Create `src-tauri/src/warnings.rs` and keep it small:
    - Define `BackendWarning`
    - Define the managed queue state wrapper using `Arc<Mutex<Vec<BackendWarning>>>`
    - Define a helper like `push_backend_warning(app: &tauri::AppHandle, warning: BackendWarning)` that stores first, then best-effort emits
    - Define `#[tauri::command] fn take_backend_warnings(...) -> Vec<BackendWarning>`

    Update `src-tauri/src/lib.rs` surgically:
    - `mod warnings;`
    - manage the warning queue during startup
    - register `take_backend_warnings` in the invoke handler
    - keep all names aligned with the research doc: event `backend-warning`, command `take_backend_warnings`

    Add small Rust unit tests around the queue contract:
    - pushing multiple warnings preserves order
    - `take_backend_warnings` drains and empties the queue
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test backend_warning_</automated>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test take_backend_warnings</automated>
  </verify>
  <done>
    - Warning queue state is managed from lib.rs
    - `take_backend_warnings` exists and drains the queue
    - Event and command naming match the phase research
  </done>
</task>

<task type="auto">
  <name>Task 2: Add minimal launcher warning surface in App.vue</name>
  <files>src/App.vue</files>
  <behavior>
    - On mount, the launcher drains pending startup warnings once
    - The launcher listens for subsequent `backend-warning` events
    - Warnings render as a simple dismissible inline banner above the search box
    - No modal, no toast framework, no Settings window work
  </behavior>
  <action>
    Update `src/App.vue` only:
    - add a lightweight warning state array
    - call `invoke('take_backend_warnings')` during startup in Tauri context
    - register and clean up a `backend-warning` listener
    - render a compact dismissible banner area above the input using existing styling patterns

    Keep the UX intentionally narrow:
    - one launcher-only surface
    - no persistence beyond the backend queue
    - no extra dependencies
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle && pnpm build</automated>
  </verify>
  <done>
    - Startup warnings are visible after launcher mount
    - Live warnings can appear without reload
    - Warning UI remains minimal and scoped to the launcher
  </done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test` remains GREEN
- `pnpm build` remains GREEN
- `backend-warning` appears in both Rust and Vue integration points
- No new notification subsystem files beyond `warnings.rs`
</verification>

<success_criteria>
- Startup-safe warning queue exists
- `take_backend_warnings` command is registered and usable from the launcher
- `App.vue` shows a dismissible backend warning banner
- Infrastructure is ready for settings and DB recovery plans without broader refactors
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-backend-resilience/09.5-01-SUMMARY.md`
</output>
