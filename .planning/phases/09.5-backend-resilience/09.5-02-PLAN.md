---
phase: 09.5-backend-resilience
plan: 02
type: execute
wave: 1
depends_on:
  - 09.5-01
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/src/store.rs
  - src-tauri/src/warnings.rs
autonomous: true
requirements:
  - "none (urgent internal hardening, no formal req IDs)"
must_haves:
  truths:
    - "Malformed or unreadable `settings.json` creates `settings.json.bak` before defaults are later persisted"
    - "Missing settings file remains a first-run path with no warning"
    - "If settings backup creation fails, Riftle does not silently overwrite the original file"
    - "Startup logic in lib.rs distinguishes normal defaults from recovery defaults so the existing immediate `set_settings()` call cannot reintroduce silent overwrite"
  artifacts:
    - path: "src-tauri/src/store.rs"
      provides: "settings load outcome type and backup-aware recovery logic"
      contains: "settings.json.bak"
    - path: "src-tauri/src/lib.rs"
      provides: "startup handshake that only persists recovered defaults when safe"
      contains: "RecoveredWithDefaults"
  key_links:
    - from: "store.rs settings load path"
      to: "warning queue from Plan 01"
      via: "returns warning payload for malformed-file recovery"
      pattern: "BackendWarning"
    - from: "lib.rs startup settings load"
      to: "subsequent set_settings() call"
      via: "branches on explicit recovery outcome instead of a bare Settings return"
      pattern: "set_settings"
---

<objective>
Replace the silent malformed-settings fallback with backup-aware recovery plus a surfaced warning, while preserving the normal first-run behavior.

Purpose: Close the settings-reset resilience gap without redesigning the settings subsystem.
Output: `store.rs` returns an explicit load outcome, `lib.rs` consumes it safely, and warnings flow through the infrastructure from Plan 01.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/09.5-backend-resilience/09.5-RESEARCH.md
@.planning/phases/09.5-backend-resilience/09.5-VALIDATION.md
@.planning/phases/09.5-backend-resilience/09.5-01-PLAN.md
@src-tauri/src/store.rs
@src-tauri/src/lib.rs
@src-tauri/src/warnings.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit settings load outcomes and backup-before-recovery semantics</name>
  <files>src-tauri/src/store.rs, src-tauri/src/warnings.rs</files>
  <behavior>
    - Distinguish `missing file`, `clean load`, `recovered with defaults`, and `fatal backup failure`
    - On malformed or unreadable existing settings file, create `settings.json.bak` in the same directory before returning defaults
    - Overwrite any prior `.bak`
    - If backup fails, return a hard failure instead of silently continuing
  </behavior>
  <action>
    Refactor `src-tauri/src/store.rs` narrowly:
    - replace the bare `Settings` load return shape with an explicit outcome enum/struct
    - keep fixed backup naming: `settings.json.bak`
    - add a small helper for backup-with-overwrite semantics if needed, but keep it local unless `db.rs` reuse in Plan 03 is trivial

    Add or update unit tests covering:
    - malformed settings file produces `.bak`
    - existing `.bak` is overwritten
    - missing file yields defaults with no warning
    - backup failure preserves the original file and returns an error outcome
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test settings_</automated>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test backup_failure_</automated>
  </verify>
  <done>
    - `store.rs` no longer silently collapses all load failures into defaults
    - backup happens before any destructive recovery path
    - tests cover malformed, missing, overwrite, and backup-failure cases
  </done>
</task>

<task type="auto">
  <name>Task 2: Update startup flow to surface settings recovery warnings safely</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/store.rs</files>
  <behavior>
    - lib.rs consumes the explicit settings load outcome
    - First run still writes defaults as before
    - Recovery-after-backup emits a backend warning and only then persists defaults
    - Fatal backup failure aborts the destructive overwrite path instead of silently continuing
  </behavior>
  <action>
    Update `src-tauri/src/lib.rs` surgically:
    - replace the current `get_settings()` + immediate `set_settings()` pattern with an outcome-aware branch
    - when recovery-with-defaults occurs, route the warning through Plan 01 infrastructure
    - when the settings file is simply absent, preserve current first-run behavior
    - keep all changes local to startup initialization; do not redesign commands or frontend settings flows
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test settings_recovery_</automated>
    <automated>cd D:/develop/projects/riftle && pnpm build</automated>
  </verify>
  <done>
    - `lib.rs` no longer silently overwrites corrupted settings through a bare default return
    - recovery warnings are enqueued for the launcher
    - first-run behavior remains unchanged
  </done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test` remains GREEN
- `pnpm build` remains GREEN
- `settings.json.bak` is named exactly as required
- No settings window UI changes are introduced
</verification>

<success_criteria>
- Settings recovery is backup-first and user-visible
- Missing settings file stays warning-free
- Backup failure is non-destructive
- Startup flow no longer reintroduces silent overwrite via immediate `set_settings()`
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-backend-resilience/09.5-02-SUMMARY.md`
</output>
