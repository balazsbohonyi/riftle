# Phase 09.5 Verification

Status: `human_needed`

Phase goal assessment: the scoped code changes for backend resilience are present and the automated verification passed. I did not find implementation gaps against the phase goal in the inspected codebase. Remaining sign-off is limited to the manual/runtime checks already identified in `09.5-VALIDATION.md`.

## Result

Automated and code-inspection verification passed for the phase scope:

- Panic-prone `.lock().unwrap()` sites in `src-tauri/src/commands.rs`, `src-tauri/src/search.rs`, and the targeted tray mutex paths in `src-tauri/src/lib.rs` were replaced with poison-tolerant recovery using `unwrap_or_else(... into_inner())`.
- `src-tauri/src/search.rs` now falls back to an empty in-memory index during `init_search_index()` load failure and preserves the previous in-memory index on `rebuild_index()` failure.
- `src-tauri/src/lib.rs` now treats `launcher.hwnd()` as fallible and logs/skips DWM cosmetic setup instead of panicking.
- `src-tauri/src/db.rs` now uses fixed-name backup-first recovery with `launcher.db.bak` before destructive reset and preserves the original DB on backup failure.
- `src-tauri/src/store.rs` now uses fixed-name backup-first recovery with `settings.json.bak`, distinguishes missing/loaded/recovered/fatal outcomes, and avoids silently overwriting a malformed settings file.
- Startup warning delivery and launcher warning rendering are wired end-to-end through `src-tauri/src/warnings.rs`, `src-tauri/src/lib.rs`, and `src/App.vue`.

## Evidence

- Roadmap phase goal and must-haves match the implemented scope in `.planning/ROADMAP.md:210` and `.planning/ROADMAP.md:223`.
- Warning queue and drain command exist in `src-tauri/src/warnings.rs:6`, `src-tauri/src/warnings.rs:32`, and `src-tauri/src/warnings.rs:42`; startup registers and uses them in `src-tauri/src/lib.rs:154`, `src-tauri/src/lib.rs:169`, `src-tauri/src/lib.rs:182`, and `src-tauri/src/lib.rs:394`.
- Launcher warning listener, startup drain, and visible banner exist in `src/App.vue:395`, `src/App.vue:398`, and `src/App.vue:467`.
- Settings recovery outcomes and `settings.json.bak` handling exist in `src-tauri/src/store.rs:95`, `src-tauri/src/store.rs:109`, `src-tauri/src/store.rs:154`, and `src-tauri/src/store.rs:165`.
- DB recovery outcome and `launcher.db.bak` handling exist in `src-tauri/src/db.rs:89` and the recovery branch around `src-tauri/src/db.rs:65`.
- Launch/search poison recovery exists in `src-tauri/src/commands.rs:14`, `src-tauri/src/search.rs:39`, and `src-tauri/src/lib.rs:102`.
- Search init/rebuild resilience exists in `src-tauri/src/search.rs:79` and `src-tauri/src/search.rs:89`.
- Fallible HWND handling exists in `src-tauri/src/lib.rs:354`.

## Automated verification run

Executed on 2026-03-12:

- `cd src-tauri && cargo test` -> passed (`75 passed`, `0 failed`, `2 ignored`)
- `pnpm build` -> passed

Notes:

- `pnpm build` initially failed inside the sandbox with `spawn EPERM` while Vite/esbuild was starting. Re-running outside the sandbox succeeded without code changes, so this was an environment restriction, not a phase regression.
- `cargo test` reported existing compiler warnings in `src-tauri/src/indexer.rs` for an unused variable and an unused assignment. These are outside Phase 09.5 scope and did not fail the suite.

## Residual risks / human checks still needed

These are not code gaps, but they were not executed during this verification:

- Corrupt `settings.json`, start Riftle, summon the launcher, and confirm both `settings.json.bak` creation and the visible launcher warning banner.
- Corrupt `launcher.db`, start Riftle, summon the launcher, and confirm both `launcher.db.bak` creation and the visible launcher warning banner.
- Verify tray single-click toggle and double-click show/focus behavior still feels correct after the tray mutex recovery changes.
- Verify launcher startup remains usable if DWM customization is skipped because `hwnd()` is unavailable.

## Conclusion

No implementation gaps were found in the scoped phase work. Phase 09.5 is code-complete and automated-check complete, but final status remains `human_needed` until the manual runtime checks above are performed.
