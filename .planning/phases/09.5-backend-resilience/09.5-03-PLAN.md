---
phase: 09.5-backend-resilience
plan: 03
type: execute
wave: 2
depends_on:
  - 09.5-02
files_modified:
  - src-tauri/src/db.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - "none (urgent internal hardening, no formal req IDs)"
must_haves:
  truths:
    - "Any destructive DB reset path creates `launcher.db.bak` first when an existing DB file is present"
    - "If DB backup creation fails, Riftle does not delete the original database"
    - "A successful DB reset queues a user-visible warning that includes the backup path"
    - "No timestamped backup fan-out is added; fixed `.bak` naming is preserved"
  artifacts:
    - path: "src-tauri/src/db.rs"
      provides: "backup-before-reset logic for launcher.db initialization failures"
      contains: "launcher.db.bak"
    - path: "src-tauri/src/lib.rs"
      provides: "startup warning enqueue after DB recovery"
      contains: "Launch history was reset"
  key_links:
    - from: "db.rs init failure path"
      to: "replacement fresh DB init"
      via: "backup succeeds before any removal/recreate step"
      pattern: "launcher.db.bak"
    - from: "db recovery outcome"
      to: "warning queue from Plan 01"
      via: "startup pushes a backend warning describing the reset"
      pattern: "BackendWarning"
---

<objective>
Add backup-before-reset behavior for the database initialization recovery path and surface the result to the launcher warning banner.

Purpose: Make launch-history loss explicit and recoverable without broad DB layer refactoring.
Output: `db.rs` backup-aware init behavior plus narrow startup warning integration.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/09.5-backend-resilience/09.5-RESEARCH.md
@.planning/phases/09.5-backend-resilience/09.5-VALIDATION.md
@.planning/phases/09.5-backend-resilience/09.5-01-PLAN.md
@.planning/phases/09.5-backend-resilience/09.5-02-PLAN.md
@src-tauri/src/db.rs
@src-tauri/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make DB reset backup-first and non-destructive on backup failure</name>
  <files>src-tauri/src/db.rs</files>
  <behavior>
    - Initial successful DB open/init path remains unchanged
    - If init fails and no DB file exists, return the original error
    - If init fails and a DB exists, create `launcher.db.bak` in the same directory before removing/recreating
    - If backup creation fails, preserve the original DB and return an error instead of deleting it
  </behavior>
  <action>
    Refactor only the recovery branch in `src-tauri/src/db.rs`:
    - keep naming fixed as `launcher.db.bak`
    - overwrite any existing backup
    - prefer same-directory move/replace semantics if straightforward, otherwise use copy-then-delete only after confirmed backup success
    - keep normal DB APIs and schema untouched outside this failure path

    Add unit tests covering:
    - corrupt/unopenable existing DB produces `.bak`
    - previous `.bak` is overwritten
    - missing DB file does not fabricate a backup
    - backup failure preserves the original DB
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test db_backup_</automated>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test backup_failure_</automated>
  </verify>
  <done>
    - `db.rs` no longer deletes an existing DB before a successful backup
    - destructive reset path is now explicitly guarded by backup success
    - tests cover the recovery branch
  </done>
</task>

<task type="auto">
  <name>Task 2: Surface DB reset warnings through the launcher warning channel</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/db.rs</files>
  <behavior>
    - DB recovery returns enough outcome information for startup to know whether a reset occurred
    - Successful backup-plus-reset queues a warning with title `Launch history was reset`
    - Warning text includes the backup path
    - Startup remains non-interactive; no modal acknowledgement is introduced
  </behavior>
  <action>
    Update the startup integration in `src-tauri/src/lib.rs` and any needed return shape in `db.rs`:
    - expose whether init was clean or recovered
    - when recovered, enqueue a backend warning through Plan 01 infrastructure
    - keep the warning launcher-only and startup-safe

    Do not expand the DB API surface beyond what startup needs for this one recovery outcome.
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test db_backup_</automated>
    <automated>cd D:/develop/projects/riftle && pnpm build</automated>
  </verify>
  <done>
    - launcher warning infrastructure now covers DB recovery too
    - warning copy and fixed backup name align with roadmap language
    - no unrelated DB refactors were added
  </done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test` remains GREEN
- `pnpm build` remains GREEN
- `launcher.db.bak` is named exactly as required
- DB reset warnings reach the launcher through existing phase warning plumbing
</verification>

<success_criteria>
- DB destructive reset is backup-first
- Backup failure leaves the original DB intact
- Launch-history reset becomes user-visible in the launcher
- Scope stays confined to the DB init recovery path
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-backend-resilience/09.5-03-SUMMARY.md`
</output>
