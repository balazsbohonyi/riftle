---
phase: 09.5-backend-resilience
plan: 04
type: execute
wave: 3
depends_on:
  - 09.5-02
  - 09.5-03
files_modified:
  - src-tauri/src/commands.rs
  - src-tauri/src/search.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - "none (urgent internal hardening, no formal req IDs)"
must_haves:
  truths:
    - "The targeted `.lock().unwrap()` sites in `commands.rs` and `search.rs` no longer crash on poisoned mutexes"
    - "Search initialization failure falls back to an empty in-memory index instead of panicking"
    - "Search rebuild failure keeps the previous in-memory index instead of replacing it with an empty one"
    - "The targeted tray timestamp mutexes in `lib.rs` recover from poison and continue"
    - "The targeted `launcher.hwnd().unwrap()` in `lib.rs` becomes fallible logging and skips cosmetic DWM setup when unavailable"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "poison-tolerant DB lock handling for launch commands"
      contains: "into_inner"
    - path: "src-tauri/src/search.rs"
      provides: "recoverable DB lock handling and non-destructive search index rebuild behavior"
      contains: "rebuild_index"
    - path: "src-tauri/src/lib.rs"
      provides: "poison-tolerant tray state handling and fallible hwnd/DWM branch"
      contains: "hwnd()"
  key_links:
    - from: "commands.rs launch path lookup"
      to: "launch command result"
      via: "logs and returns recoverably instead of panicking on poisoned DB mutex"
      pattern: "lock"
    - from: "search.rs rebuild_index"
      to: "existing in-memory search index"
      via: "failed DB refresh leaves previous state intact"
      pattern: "rebuild_index"
    - from: "lib.rs DWM customization"
      to: "launcher startup"
      via: "missing hwnd logs and skips cosmetic setup only"
      pattern: "hwnd"
---

<objective>
Harden the exact panic-prone runtime unwrap sites called out in Phase 09.5, without expanding into a broader repository-wide unwrap audit.

Purpose: Convert rare backend crashes into logged recoverable failures in `commands.rs`, `search.rs`, and `lib.rs`.
Output: targeted unwrap replacements only in the named files.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/09.5-backend-resilience/09.5-RESEARCH.md
@.planning/phases/09.5-backend-resilience/09.5-VALIDATION.md
@.planning/phases/09.5-backend-resilience/09.5-02-PLAN.md
@.planning/phases/09.5-backend-resilience/09.5-03-PLAN.md
@src-tauri/src/commands.rs
@src-tauri/src/search.rs
@src-tauri/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace targeted poisoned DB mutex unwraps in commands.rs and search.rs</name>
  <files>src-tauri/src/commands.rs, src-tauri/src/search.rs</files>
  <behavior>
    - `commands.rs` path lookup and launch-count update no longer panic on poisoned DB mutexes
    - Launch-count update failure remains log-and-continue
    - `init_search_index()` logs DB failures and initializes an empty index
    - `rebuild_index()` logs DB failures and preserves the existing in-memory index
  </behavior>
  <action>
    Edit only the targeted sites named in the research:
    - in `commands.rs`, replace `.lock().unwrap()` with poison-tolerant handling using `into_inner()` and current forgiving command behavior
    - in `search.rs`, replace the DB mutex unwrap sites with poison-tolerant handling
    - keep the already-established app behavior style: log, recover, continue

    Add or adjust focused tests where practical:
    - search rebuild failure keeps prior state
    - existing search tests remain GREEN

    Do not widen this task into unrelated `indexer.rs` or general unwrap cleanup.
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test rebuild_index</automated>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test search_</automated>
  </verify>
  <done>
    - no targeted DB mutex unwraps remain in `commands.rs` and `search.rs`
    - search behavior on DB failure matches the recovery strategy from research
    - all relevant tests remain GREEN
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace targeted lib.rs tray mutex and hwnd unwraps with fallible handling</name>
  <files>src-tauri/src/lib.rs</files>
  <behavior>
    - tray bookkeeping mutexes recover from poison and continue
    - `launcher.hwnd()` becomes an explicit `match` or equivalent fallible branch
    - failed hwnd acquisition logs and skips DWM border/corner customization only
    - no startup crash on cosmetic handle failure
  </behavior>
  <action>
    Edit the exact `lib.rs` sites called out in research:
    - tray `pending_left_click` and `suppress_left_click_until` mutex accesses
    - the DWM customization block that currently unwraps `launcher.hwnd()`

    Keep the behavior narrow:
    - recover poison with `into_inner()`
    - log failure at each site
    - skip cosmetic DWM setup when no HWND is available

    Do not alter tray behavior semantics beyond error handling.
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test</automated>
    <automated>cd D:/develop/projects/riftle && pnpm build</automated>
  </verify>
  <done>
    - targeted tray and hwnd unwraps are gone
    - launcher startup remains resilient when DWM customization is skipped
    - phase scope stayed limited to the named files
  </done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test` remains GREEN
- `pnpm build` remains GREEN
- The named unwrap sites in `commands.rs`, `search.rs`, and `lib.rs` are handled fallibly
- No unrelated unwrap audit was added outside the listed files
</verification>

<success_criteria>
- Panic-prone runtime unwraps in the scoped files are replaced with recoverable handling
- Search rebuild failure preserves prior in-memory state
- DWM handle failure is cosmetic-only
- The plan remains tightly scoped to the roadmap insertion
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-backend-resilience/09.5-04-SUMMARY.md`
</output>
