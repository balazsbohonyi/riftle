---
phase: 09.5-backend-resilience
plan: 05
type: execute
wave: 4
depends_on:
  - 09.5-01
  - 09.5-02
  - 09.5-03
files_modified:
  - src/App.vue
  - src-tauri/src/lib.rs
  - src-tauri/src/store.rs
  - src/styles/tokens.css
autonomous: true
requirements:
  - "none (gap closure for diagnosed warning-banner cosmetic issue)"
must_haves:
  truths:
    - "Backend recovery warnings expose the backup path from a single source of truth instead of duplicating it in both `message` and `backup_path`"
    - "The launcher warning banner renders the backup path only once"
    - "The displayed backup path remains legible but lighter, close to white, instead of the current dim gray monospace treatment"
    - "The warning banner remains dismissible and does not leave broken spacing or block search interaction"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "DB recovery warning payload with non-duplicated path copy"
      contains: "startup_db_warning"
    - path: "src-tauri/src/store.rs"
      provides: "settings recovery warning payload with non-duplicated path copy"
      contains: "RecoveredWithDefaults"
    - path: "src/App.vue"
      provides: "single-render warning banner copy and backup path presentation"
      contains: "warning-path"
    - path: "src/styles/tokens.css"
      provides: "lighter warning-path text token"
      contains: "--color-text"
  key_links:
    - from: "store/lib backend warning builders"
      to: "App.vue warning banner"
      via: "message carries recovery explanation while `backup_path` carries the only rendered file path"
      pattern: "backup_path"
    - from: "App.vue warning template"
      to: "UAT expectation"
      via: "banner shows one backup path with lighter styling"
      pattern: "warning.backup_path"
---

<objective>
Close the diagnosed Phase 09.5 UAT gap in the backend recovery warning banner only.

Purpose: remove duplicate backup-path copy from recovery warnings and align the rendered backup path styling with the reported expectation.
Output: one focused gap-closure change across the existing warning payload builders and banner presentation.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09.5-backend-resilience/09.5-UAT.md
@src/App.vue
@src-tauri/src/lib.rs
@src-tauri/src/store.rs
@src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make backend recovery warnings use a single backup-path source of truth</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/store.rs</files>
  <behavior>
    - DB recovery warnings keep the explanatory recovery message but stop embedding the backup path inside `message`
    - settings recovery warnings do the same, preserving `backup_path` as the only file-path field
    - warning payload shape stays compatible with the existing frontend warning model
  </behavior>
  <action>
    Edit only the recovery warning construction sites:
    - in `src-tauri/src/lib.rs`, update `startup_db_warning()` so `message` explains the reset/recovery without repeating the backup file path
    - in `src-tauri/src/store.rs`, update the `RecoveredWithDefaults` warning payload so `message` no longer includes the backup file path text

    Keep `backup_path` populated in both payloads.

    Add or adjust focused Rust tests so the warning-copy contract is explicit:
    - warning `backup_path` remains present
    - warning `message` no longer contains the backup filename/path string

    Do not change unrelated resilience behavior, warning queueing, or recovery flow.
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test startup_db_warning</automated>
    <automated>cd D:/develop/projects/riftle/src-tauri && cargo test settings_</automated>
  </verify>
  <done>
    - recovery warnings still identify what was reset/recovered
    - `backup_path` remains available for the UI
    - duplicate backup-path copy is removed at the payload source
  </done>
</task>

<task type="auto">
  <name>Task 2: Render and style the backup path once in the launcher warning banner</name>
  <files>src/App.vue, src/styles/tokens.css</files>
  <behavior>
    - the banner still shows title, explanatory message, and backup path when present
    - the backup path is rendered only once
    - the rendered path no longer looks like a dim gray developer-style secondary dump; it reads as a clear user-facing detail
    - banner dismissal and layout sizing behavior stay intact
  </behavior>
  <action>
    Update `src/App.vue` narrowly around the warning banner:
    - preserve the current banner structure and dismissal flow
    - ensure the path is presented from `warning.backup_path` only once
    - keep the explanatory sentence and the path visually distinct without duplicating content

    Update `src/styles/tokens.css` and/or the local warning-path rule so the rendered path is lighter, near-white in dark mode, while remaining readable in light mode.

    Do not redesign the warning system or alter unrelated launcher styling.
  </action>
  <verify>
    <automated>cd D:/develop/projects/riftle && pnpm build</automated>
    <manual>Trigger both malformed `settings.json` and corrupt `launcher.db` recovery flows, summon the launcher, and confirm the banner shows one backup path only, with lighter readable styling and clean dismissal spacing.</manual>
  </verify>
  <done>
    - the launcher banner presents one backup path only
    - the path styling matches the UAT feedback more closely
    - search input and banner dismissal behavior still work normally
  </done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test` remains GREEN
- `pnpm build` remains GREEN
- Recovery warnings contain explanatory text plus a single UI-rendered backup path
- Manual runtime check confirms the duplicate path is gone for both settings and DB recovery banners
</verification>

<success_criteria>
- The diagnosed warning-banner gap from `09.5-UAT.md` is fully closed without expanding phase scope
- The backup path is represented once, from one source of truth
- The rendered backup path is lighter and easier to read
- The existing warning banner dismissal and launcher usability remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-backend-resilience/09.5-05-SUMMARY.md`
</output>
