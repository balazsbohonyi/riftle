---
phase: 09.5
slug: backend-resilience
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-11
---

# Phase 09.5 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in (`#[test]`) |
| **Config file** | none - `cargo test` discovers all `#[cfg(test)]` modules |
| **Quick run command** | `cd src-tauri && cargo test` |
| **Full suite command** | `cd src-tauri && cargo test` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd src-tauri && cargo test`
- **After every plan wave:** Run `cd src-tauri && cargo test`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Concern | Test Type | Automated Command | File Exists | Status |
|---------|------|------|---------|-----------|-------------------|-------------|--------|
| 09.5-01-01 | 01 | 0 | Warning queue drain/event payload contract | unit | `cd src-tauri && cargo test backend_warning_` | ❌ W0 | ⬜ pending |
| 09.5-01-02 | 01 | 0 | Launcher consumes queued startup warnings and renders minimal banner path | build | `cd D:/develop/projects/riftle && pnpm build` | ✅ existing App.vue path | ⬜ pending |
| 09.5-02-01 | 02 | 1 | Settings malformed-file recovery creates `settings.json.bak` and returns explicit recovery outcome | unit | `cd src-tauri && cargo test settings_` | ✅ in plan 02 | ⬜ pending |
| 09.5-02-02 | 02 | 1 | Missing settings file remains first-run path with no warning | unit | `cd src-tauri && cargo test settings_missing` | ✅ in plan 02 | ⬜ pending |
| 09.5-02-03 | 02 | 1 | Backup failure preserves the original settings file and blocks silent overwrite | unit | `cd src-tauri && cargo test backup_failure_` | ✅ in plan 02 | ⬜ pending |
| 09.5-02-04 | 02 | 1 | Startup flow branches on explicit recovery outcomes and surfaces settings warnings safely | unit | `cd src-tauri && cargo test settings_recovery_` | ✅ in plan 02 | ⬜ pending |
| 09.5-03-01 | 03 | 2 | DB init failure creates `launcher.db.bak` before reset | unit | `cd src-tauri && cargo test db_backup_` | ✅ in plan 03 | ⬜ pending |
| 09.5-03-02 | 03 | 2 | Backup failure preserves original DB and prevents destructive reset | unit | `cd src-tauri && cargo test backup_failure_` | ✅ in plan 03 | ⬜ pending |
| 09.5-03-03 | 03 | 2 | DB recovery warning reaches launcher through the warning queue/event path | build | `cd D:/develop/projects/riftle && pnpm build` | ✅ via plans 01 + 03 | ⬜ pending |
| 09.5-04-01 | 04 | 3 | Targeted `commands.rs`, `search.rs`, and `lib.rs` unwrap paths are recoverable | unit | `cd src-tauri && cargo test` | ✅ compile + suite | ⬜ pending |
| 09.5-04-02 | 04 | 3 | Search index rebuild failure keeps prior in-memory state | unit | `cd src-tauri && cargo test rebuild_index` | ✅ in plan 04 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠ flaky*

---

## Wave 0 Requirements

- [ ] `src-tauri/src/lib.rs` or `src-tauri/src/warnings.rs` - add pending warning queue tests and drain command contract tests

*Wave 0 in Phase 09.5 is intentionally limited to warning infrastructure. DB and settings recovery tests are owned by Plans 02 and 03 rather than separate RED-only stubs.*

---

## Manual-Only Verifications

| Behavior | Concern | Why Manual | Test Instructions |
|----------|---------|------------|-------------------|
| Corrupted `settings.json` produces visible launcher warning after startup | Item 7 (settings reset is user-visible) | Requires real startup timing across Rust emit + Vue mount | Corrupt the active `settings.json`, start Riftle, summon launcher, verify warning banner appears and `settings.json.bak` exists beside the active file |
| Corrupted `launcher.db` produces visible launcher warning after startup | Item 7 (DB reset is user-visible) | Requires real DB reset path plus launcher UI confirmation | Corrupt the active `launcher.db`, start Riftle, summon launcher, verify warning banner appears and `launcher.db.bak` exists |
| Tray click/double-click behavior still works after mutex poison recovery changes | Item 8 (lib.rs tray mutex unwrap replacement) | Behavior depends on runtime tray interactions, not just compilation | Run app with tray enabled; verify single-click toggle and double-click show/focus still behave as before |
| Launcher still starts if DWM customization is skipped | Item 8 (`hwnd()` fallible handling) | Hard to force handle acquisition failure deterministically in unit tests | Run debug build and confirm launcher still opens if DWM customization path logs a handle warning or is bypassed |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 10s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready for execution 2026-03-11
