---
phase: 09.6-ux-safety-gates
plan: "03"
subsystem: ui
tags: [vue, typescript, hotkey, keycapture, settings]

# Dependency graph
requires:
  - phase: 09.6-01
    provides: update_hotkey Tauri command with two-phase swap and error return
  - phase: 09.6-02
    provides: confirmation overlay infrastructure in Settings.vue
provides:
  - lastRegisteredHotkey ref tracking actual backend hotkey state in Settings.vue
  - @keydown.prevent defensive hardening on KeyCapture div
affects: [09.6-04]

# Tech tracking
tech-stack:
  added: []
  patterns: [track-backend-state-separately-from-v-model]

key-files:
  created: []
  modified:
    - src/Settings.vue
    - src/components/ui/KeyCapture.vue

key-decisions:
  - "lastRegisteredHotkey ref is the source of truth for the previous working hotkey — updated only on successful invoke, never on v-model updates which fire before change"
  - "@keydown.prevent on KeyCapture div makes preventDefault unconditional at DOM level, firing even when not in capturing state"

patterns-established:
  - "Separate ref pattern: when v-model fires before the change handler, track authoritative state in a dedicated ref updated only after confirmed backend success"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-03-12
---

# Phase 09.6 Plan 03: Hotkey Error Feedback Fix Summary

**Fixed stale oldHotkey snapshot in Settings.vue so hotkey error messages show the correct previous working key and revert is no longer a no-op; added @keydown.prevent to KeyCapture for unconditional DOM-level event suppression.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-12T19:47:31Z
- **Completed:** 2026-03-12T19:48:54Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Fixed the stale `oldHotkey` snapshot bug: `lastRegisteredHotkey` ref now holds the actual backend-confirmed hotkey and is used as the error message source and revert target
- Synced `lastRegisteredHotkey` from `response.hotkey` in `onMounted` so initial state matches backend
- Added `@keydown.prevent` modifier to `.key-capture` div in KeyCapture.vue for unconditional DOM-level event suppression

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix lastRegisteredHotkey tracking in Settings.vue** - `1d2ed61` (feat)
2. **Task 2: Add @keydown.prevent to KeyCapture div** - `d68fb38` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `src/Settings.vue` - Added `lastRegisteredHotkey` ref, synced in onMounted, used in onHotkeyChange as old-value source and revert target
- `src/components/ui/KeyCapture.vue` - Changed `@keydown` to `@keydown.prevent` on .key-capture div

## Decisions Made
- `lastRegisteredHotkey` is initialized to `'Alt+Space'` (the default hotkey) and overwritten by `response.hotkey` in onMounted — this means the very first failed registration attempt before settings load still shows a coherent fallback message
- `lastRegisteredHotkey.value` is updated on success only; failure path leaves it unchanged so `settings.value.hotkey = oldHotkey` triggers the KeyCapture prop watcher to visually revert the display

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Gap 1 (hotkey error feedback) is closed: error message correctly names the rejected key and the still-active previous hotkey; revert is functional
- Gap 2 Vue side is closed: KeyCapture div has unconditional DOM-level key suppression
- Plan 04 (Rust-side WM_SYSCOMMAND / Alt+Space OS interference fix) can proceed independently

---
*Phase: 09.6-ux-safety-gates*
*Completed: 2026-03-12*
