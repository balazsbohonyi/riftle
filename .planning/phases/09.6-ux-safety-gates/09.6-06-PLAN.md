---
phase: 09.6-ux-safety-gates
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.vue
autonomous: false
gap_closure: true
requirements: []

must_haves:
  truths:
    - "ESC dismisses the inline confirmation row and restores the search input and results list"
    - "Enter on the focused confirm button executes the command through the intended onKeyDown code path"
    - "Cancel is reachable via keyboard (Escape) without requiring Tab navigation"
  artifacts:
    - path: "src/App.vue"
      provides: "keydown listener on .launcher-app root div"
      contains: "@keydown=\"onKeyDown\""
  key_links:
    - from: "src/App.vue .launcher-app div"
      to: "onKeyDown handler"
      via: "@keydown event bubbling from confirm button to root"
      pattern: "@keydown=\"onKeyDown\""
---

<objective>
Fix keyboard handling scope during inline confirmation so Escape dismisses and Enter confirms through the intended onKeyDown code path.

Purpose: All three remaining verification gaps (ESC dead, Enter by accident, Cancel keyboard-only) share a single root cause — onKeyDown is attached to the `<input>` element which is unmounted when confirmPending=true. The logic inside onKeyDown already handles confirmPending correctly (lines 272-275 cancel, lines 293-295 confirm). The handler just needs to be reachable.

Output: One-line template change to App.vue that makes Escape and Enter work during confirmation via the existing onKeyDown logic.
</objective>

<execution_context>
@C:/Users/Balazs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09.6-ux-safety-gates/09.6-VERIFICATION.md

<interfaces>
<!-- Key facts from VERIFICATION.md — no codebase exploration needed -->

App.vue line 508 (current):
```html
<div class="launcher-app" :class="[`anim-${animMode}`, { visible: isVisible }]" @contextmenu.prevent="onContextMenu">
```

App.vue line 547 (current — keydown only on input):
```html
<input @keydown="onKeyDown" ... v-if="!confirmPending" ... />
```

onKeyDown cancel branch (lines 272-275):
```typescript
if (confirmPending.value) {
  cancelConfirm()
  return
}
```

onKeyDown enter branch (lines 293-295):
```typescript
if (confirmPending.value) {
  confirmAction()
  return
}
```

Required fix — line 508 becomes:
```html
<div class="launcher-app" :class="[`anim-${animMode}`, { visible: isVisible }]" @contextmenu.prevent="onContextMenu" @keydown="onKeyDown">
```

No other changes needed. The onKeyDown logic already handles confirmPending correctly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @keydown handler to .launcher-app root div</name>
  <files>src/App.vue</files>
  <action>
    On the `.launcher-app` root div (line 508 in template), add `@keydown="onKeyDown"` as an additional attribute.

    Current line 508:
    ```html
    <div class="launcher-app" :class="[`anim-${animMode}`, { visible: isVisible }]" @contextmenu.prevent="onContextMenu">
    ```

    Updated line 508:
    ```html
    <div class="launcher-app" :class="[`anim-${animMode}`, { visible: isVisible }]" @contextmenu.prevent="onContextMenu" @keydown="onKeyDown">
    ```

    Do NOT remove `@keydown="onKeyDown"` from the `<input>` element at line 547 — keep it there as well. The root div handler catches events when the input is unmounted (confirmPending=true); the input handler catches events in the normal search flow. Both coexist without conflict because the input's own handler fires first via natural DOM propagation and then bubbles to the root.

    No changes to onKeyDown logic, no changes to confirmPending logic, no changes to CSS. This is a one-attribute addition.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    - `pnpm build` exits 0 (no TypeScript errors, no Vite errors)
    - `src/App.vue` line 508 contains both `@contextmenu.prevent="onContextMenu"` and `@keydown="onKeyDown"` on the `.launcher-app` div
    - The `<input @keydown="onKeyDown">` at line 547 is still present (not removed)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify keyboard handling during confirmation</name>
  <files></files>
  <action>Human verification of Escape and Enter behavior during the inline confirmation row.</action>
  <what-built>
    Added @keydown="onKeyDown" to the .launcher-app root div so keyboard events bubble to the handler regardless of which element has focus — specifically when the search input is unmounted during confirmPending=true.
  </what-built>
  <how-to-verify>
    Run `pnpm tauri dev` and test these scenarios:

    1. Escape dismisses confirmation:
       - Type `> shutdown` in the launcher
       - Press Enter — the inline confirm row appears
       - Press Escape
       - Expected: confirm row disappears, search input reappears, results list restores

    2. Enter confirms from keyboard:
       - Type `> shutdown`, press Enter (confirm row appears)
       - Press Enter again without any mouse interaction
       - Expected: launcher hides (skip actually shutting down — cancel quickly or use restart scenario without completing it)

    3. Restart confirmation same behavior:
       - Type `> restart`, press Enter — confirm row appears
       - Press Escape — row dismisses, launcher restores normally

    4. Normal search unaffected:
       - Type any app name, navigate with arrow keys, press Enter
       - Expected: app launches normally
  </how-to-verify>
  <verify>Human approval required</verify>
  <done>All four scenarios confirmed working by user.</done>
  <resume-signal>Type "approved" if all scenarios work, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Build check:
```
pnpm build
```

Grep check (run from project root):
```
grep -n "@keydown" src/App.vue
```
Expected: two lines — one on `.launcher-app` div (~line 508) and one on the `<input>` (~line 547).
</verification>

<success_criteria>
- `pnpm build` passes with 0 errors
- Escape during inline confirmation dismisses the confirm row (previously dead code path now reachable)
- Enter during inline confirmation executes via onKeyDown path (not just browser button default)
- Normal launcher keyboard navigation unaffected
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/09.6-ux-safety-gates/09.6-06-SUMMARY.md`
</output>
