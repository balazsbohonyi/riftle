---
phase: 9.6
slug: ux-safety-gates
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-12
---

# Phase 9.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` (no frontend test framework) |
| **Config file** | `src-tauri/Cargo.toml` |
| **Quick run command** | `cd src-tauri && cargo test` |
| **Full suite command** | `cd src-tauri && cargo test && pnpm build` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd src-tauri && cargo test`
- **After every plan wave:** Run `cd src-tauri && cargo test && pnpm build`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| hotkey-swap | 01 | 1 | atomic swap | unit (Rust) | `cd src-tauri && cargo test` | ✅ | ⬜ pending |
| hotkey-err-return | 01 | 1 | err on fail | unit (Rust) | `cd src-tauri && cargo test` | ✅ | ⬜ pending |
| confirm-overlay | 02 | 1 | overlay appears | manual | N/A — UI | N/A | ⬜ pending |
| esc-cancel | 02 | 1 | ESC restores state | manual | N/A — UI | N/A | ⬜ pending |
| enter-confirm | 02 | 1 | Enter executes | manual | N/A — destructive | N/A | ⬜ pending |
| hotkey-inline-err | 01 | 1 | inline error shown | manual | N/A — UI | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test files needed before implementation begins.

- `cargo test` already runs existing `hotkey.rs` and `search.rs` unit tests
- New Rust unit tests for `update_hotkey` two-phase logic will be added in the implementation plan

*Note: `GlobalShortcutExt` from the Tauri plugin is not easily mockable — hotkey swap correctness will be verified by unit tests on the logic structure and by manual OS-level testing.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirmation overlay appears for shutdown | gate scope | Vue UI overlay; no test framework | Run app, type `> shutdown`, press Enter, verify overlay appears |
| Confirmation overlay appears for restart | gate scope | Vue UI overlay; no test framework | Run app, type `> restart`, press Enter, verify overlay appears |
| ESC cancels overlay, query/result preserved | keyboard behavior | UI state verification | Open overlay, press Escape, verify search state unchanged |
| Enter confirms, launcher hides, command runs | keyboard behavior | Destructive action | Open overlay, press Enter, verify launcher hides and action runs |
| Lock/Sleep bypass confirmation | gate scope | UI behavior | Type `> lock`, press Enter, verify no overlay (direct execute) |
| Settings.vue shows inline error on bad hotkey | hotkey error feedback | UI behavior | Set an already-used hotkey in settings, verify error text appears |
| KeyCapture reverts to old hotkey on error | hotkey error feedback | UI behavior | Set bad hotkey, verify KeyCapture shows old key after error |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
