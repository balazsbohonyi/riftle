---
status: complete
phase: 09.6-ux-safety-gates
source: 09.6-01-SUMMARY.md, 09.6-02-SUMMARY.md
started: 2026-03-12T00:00:00Z
updated: 2026-03-13T00:09:46Z
---

## Final Retest Addendum

Retested on 2026-03-13 after follow-up fixes to `App.vue`, `KeyCapture.vue`, `Settings.vue`, `hotkey.rs`, and `lib.rs`.

- `Alt+Space` is captured correctly in the Settings hotkey field and no longer opens the native Windows system menu.
- Hotkey registration failure now surfaces a readable inline error message, verified with `Alt+Shift+Space`.
- Shutdown and restart both show the inline confirmation row on first `Enter`.
- `Escape`, `Tab`, `Enter`, and `ArrowLeft` / `ArrowRight` all behave correctly inside the inline confirmation row.
- Lock and Sleep still bypass confirmation.

Final manual result: approved.

## Current Test

[testing complete]

## Tests

### 1. Hotkey registration failure — inline error
expected: Open Settings → Hotkey section. Try changing the hotkey to a key already claimed by another app or OS (e.g. Win+D, PrintScreen, or any key Windows reserves). The KeyCapture input should revert to the previous hotkey automatically, and a red error message should appear below the Hotkey row saying something like "'<key>' could not be registered" and "still using <old hotkey>".
result: issue
reported: "the KeyCapture input reverts, but I don't see the error message"
severity: major

### 2. Successful hotkey change
expected: In Settings → Hotkey, change to an available hotkey (e.g. Alt+F9 or Ctrl+Alt+R). No error message shown. Close Settings. The new hotkey should summon/hide the launcher.
result: issue
reported: "Ctrl+Alt+Space worked. But when trying to reset back by pressing Alt+Space in the KeyCapture input, the OS window context menu appeared instead of capturing Alt+Space"
severity: major

### 3. Shutdown confirmation overlay
expected: In the launcher, type `> shutdown` and press Enter. Instead of executing immediately, a modal overlay should appear with a dark backdrop and a card asking "Shut down Windows?" The confirm (danger) button should be auto-focused so pressing Enter again would confirm.
result: issue
reported: "The overlay appears but is clipped at the top by the launcher field (should work like the context menu which is not clipped). ESC key did not dismiss it. Tab could switch to Cancel button but pressing Enter on Cancel did nothing."
severity: major

### 4. Restart confirmation overlay
expected: Type `> restart` and press Enter. A confirmation overlay should appear with "Restart Windows?" and a focused confirm button.
result: issue
reported: "Same problems as shutdown: overlay clipped at top, ESC didn't work, Cancel button unresponsive"
severity: major

### 5. Escape cancels confirmation overlay
expected: While the confirmation overlay is visible, press Escape. The overlay disappears, focus returns to the search input, and no system command is executed.
result: issue
reported: "ESC key did not dismiss the overlay (confirmed during test 3 and 4)"
severity: major

### 6. Cancel button dismisses overlay
expected: Open the shutdown confirmation overlay (> shutdown + Enter). Click the Cancel button. The overlay closes without executing anything.
result: issue
reported: "Cancel button focused via Tab but pressing Enter on it did nothing (confirmed during test 3)"
severity: major

### 7. Outside click does not dismiss overlay
expected: While the confirmation overlay is open, click on the dark backdrop area outside the card. The overlay stays open — it should NOT close or cancel on an outside click.
result: skipped
reason: Overlay design is being replaced entirely — not applicable to new design

### 8. Lock and Sleep bypass confirmation
expected: Type `> lock` and press Enter → executes immediately (screen locks) without any confirmation overlay. Same for `> sleep` — runs without a dialog.
result: skipped
reason: Cannot safely test during UAT; will verify after redesign is implemented

## Summary

total: 8
passed: 0
issues: 6
pending: 0
skipped: 2

## Gaps

- truth: "Red error message appears below the Hotkey row when registration fails, showing the rejected key and confirming the old hotkey is still active"
  status: failed
  reason: "User reported: the KeyCapture input reverts, but I don't see the error message"
  severity: major
  test: 1
  root_cause: "KeyCapture emits update:modelValue before change, so settings.value.hotkey is already overwritten with the new (failing) hotkey by the time onHotkeyChange captures oldHotkey on line 105 — revert becomes a no-op and the error message is incoherent/not rendered meaningfully"
  artifacts:
    - path: "src/Settings.vue"
      line: 105
      issue: "oldHotkey captured AFTER v-model has already written new hotkey via update:modelValue — snapshot is stale"
    - path: "src/Settings.vue"
      line: 116
      issue: "settings.value.hotkey = oldHotkey assigns the failing hotkey back to itself — no revert"
    - path: "src/components/ui/KeyCapture.vue"
      line: "32-33"
      issue: "emit('update:modelValue') fires before emit('change'), corrupting the oldHotkey snapshot in the parent"
  missing:
    - "Add lastRegisteredHotkey ref in Settings.vue, updated only on successful invoke; use it as oldHotkey source and for revert"

- truth: "KeyCapture input captures all hotkey combinations including Alt+Space without OS interference"
  status: failed
  reason: "User reported: pressing Alt+Space in the KeyCapture input triggered the OS window context menu instead of being captured"
  severity: major
  test: 2
  root_cause: "Windows dispatches WM_SYSCOMMAND SC_KEYMENU before the DOM keydown event fires for Alt+Space; JS preventDefault() is called too late and cannot suppress the OS-level system menu"
  artifacts:
    - path: "src/components/ui/KeyCapture.vue"
      line: 17
      issue: "e.preventDefault() cannot suppress WM_SYSCOMMAND which fires before the keydown DOM event"
    - path: "src/components/ui/KeyCapture.vue"
      line: 39
      issue: "@keydown has no .prevent modifier (minor hardening gap)"
    - path: "src-tauri/tauri.conf.json"
      line: "30-39"
      issue: "No Rust-side WM_SYSCOMMAND hook to intercept the system menu before WebView2 sees it"
  missing:
    - "Rust: subclass settings HWND with SetWindowSubclass to swallow WM_SYSCOMMAND where wParam & 0xFFF0 == SC_KEYMENU (0xF100)"
    - "Vue: add @keydown.prevent to KeyCapture div as defensive hardening"

- truth: "Confirmation UX for shutdown/restart is an inline single-line prompt inside the launcher field (not a floating overlay)"
  status: failed
  reason: "User requested redesign: remove overlay+backdrop entirely; show confirmation inline in the launcher field as one line: '<Message> [Confirm] [Cancel]'; hide search text and result list while confirming; restore both on cancel; no secondary body text — just 'Shut down Windows?' or 'Restart Windows?'"
  severity: major
  test: 3
  root_cause: "The modal overlay approach had three compounding bugs (position:fixed clipped by transform containing block, ESC handler on <input> only so lost when focus moves to buttons, Cancel @mousedown.prevent swallowing click) — user rejected the overlay design entirely in favour of an inline confirmation row inside the launcher field"
  artifacts:
    - path: "src/App.vue"
      line: 684
      issue: "transform: translateY on .launcher-app creates new containing block — position:fixed children are clipped to it"
    - path: "src/App.vue"
      line: 545
      issue: "@keydown handler on <input> only; focus moves to confirm button so handler is unreachable"
    - path: "src/App.vue"
      line: "638-643"
      issue: "Cancel button uses @mousedown.prevent only — no @click, no @keydown.enter; mousedown.prevent swallows activation"
  missing:
    - "Remove .confirm-backdrop and .confirm-card overlay entirely from App.vue template and CSS"
    - "Add inline confirmation row inside the launcher field: single line showing 'Shut down Windows?' or 'Restart Windows?' with [Confirm] and [Cancel] buttons"
    - "While confirmPending: hide search input text and results list; show inline confirmation row"
    - "On cancel: restore previous search text and results list"
    - "ESC and Enter keyboard handling already correct in logic — will work once focus stays within existing keyboard handler scope"
