---
phase: 01-project-scaffold-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src/App.vue
autonomous: false
requirements:
  - SCAF-02
  - SCAF-04

must_haves:
  truths:
    - "tauri.conf.json declares two windows: launcher (frameless, transparent, shadow, no taskbar, always-on-top, hidden at start) and settings (normal framed, hidden at start)"
    - "Bundle identifier is com.riftle.launcher"
    - "capabilities/default.json covers both launcher and settings window labels with all plugin permissions"
    - "App.vue is a minimal transparent shell with no greet imports, no errors when loaded"
    - "JS plugin packages (@tauri-apps/plugin-store, @tauri-apps/plugin-global-shortcut, @tauri-apps/plugin-autostart) are installed"
    - "`pnpm tauri dev` compiles and starts without errors (smoke test)"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Two-window Tauri app configuration"
      contains: "\\\"label\\\": \\\"launcher\\\""
    - path: "src-tauri/capabilities/default.json"
      provides: "IPC permissions for both windows"
      contains: "\\\"launcher\\\", \\\"settings\\\""
    - path: "src/App.vue"
      provides: "Minimal launcher shell — no errors, transparent background"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src-tauri/capabilities/default.json"
      via: "window labels must match — launcher and settings appear in both files"
      pattern: "launcher.*settings"
    - from: "src/App.vue"
      to: "tauri.conf.json transparent: true"
      via: "CSS body { background: transparent } required or window is white"
      pattern: "background.*transparent"
---

<objective>
Rewrite tauri.conf.json for the two-window app, update capabilities/default.json to cover both window labels with all plugin permissions, replace App.vue with a transparent minimal shell, install JS plugin packages, and verify the full stack starts with `pnpm tauri dev`.

Purpose: Satisfies SCAF-02 (both windows declared with correct flags) and SCAF-04 (clean dev start). The launcher window starts hidden — it will be shown via hotkey in Phase 9.

Output: Working two-window Tauri app skeleton that starts cleanly with `pnpm tauri dev`.
</objective>

<execution_context>
@C:/Users/Balazs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-scaffold-configuration/01-RESEARCH.md
@.planning/phases/01-project-scaffold-configuration/01-01-SUMMARY.md

<interfaces>
<!-- Current state of files being replaced. Read before writing. -->

Current src-tauri/tauri.conf.json (needs full rewrite):
- Single unnamed window, 800x600, no flags, identifier: com.balazs.bohonyi.riftle

Current src-tauri/capabilities/default.json (needs update):
- "windows": ["main"] — must change to ["launcher", "settings"]
- "permissions": ["core:default", "opener:default"] — must add store, global-shortcut, autostart

Window flag reference (all camelCase in Tauri v2 JSON — NOT snake_case):
- skipTaskbar (not skip_taskbar)
- alwaysOnTop (not always_on_top)
- decorations: false = frameless
- transparent: true — requires body { background: transparent } in CSS or window shows white
- shadow: true — provides floating appearance (deliberate per user decision)
- visible: false — both windows start hidden
- focus: false — launcher does not steal focus on startup

JS packages needed (Rust plugins require matching npm packages for TypeScript bindings):
- @tauri-apps/plugin-store
- @tauri-apps/plugin-global-shortcut
- @tauri-apps/plugin-autostart
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite tauri.conf.json, update capabilities, install JS plugin packages</name>
  <files>
    src-tauri/tauri.conf.json,
    src-tauri/capabilities/default.json
  </files>
  <action>
Rewrite src-tauri/tauri.conf.json with the complete two-window configuration:

```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "riftle",
  "version": "0.1.0",
  "identifier": "com.riftle.launcher",
  "build": {
    "beforeDevCommand": "pnpm dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "pnpm build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "label": "launcher",
        "title": "Riftle",
        "width": 640,
        "height": 60,
        "decorations": false,
        "transparent": true,
        "shadow": true,
        "resizable": false,
        "skipTaskbar": true,
        "alwaysOnTop": true,
        "visible": false,
        "focus": false
      },
      {
        "label": "settings",
        "title": "Riftle Settings",
        "width": 800,
        "height": 600,
        "minWidth": 600,
        "minHeight": 400,
        "decorations": true,
        "visible": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
```

Critical details (all camelCase — do NOT use snake_case):
- Launcher: decorations false, transparent true, shadow true, skipTaskbar true, alwaysOnTop true, visible false, focus false, width 640, height 60
- Settings: decorations true, minWidth 600, minHeight 400, visible false
- identifier changed from com.balazs.bohonyi.riftle to com.riftle.launcher (per user decision)
- Both windows load the same index.html by default — no url field needed for Phase 1

Then rewrite src-tauri/capabilities/default.json:

```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for launcher and settings windows",
  "windows": ["launcher", "settings"],
  "permissions": [
    "core:default",
    "opener:default",
    "store:default",
    "global-shortcut:default",
    "autostart:default"
  ]
}
```

Critical: windows array must be ["launcher", "settings"] — NOT ["main"]. If left as ["main"], no window matches and all IPC invoke() calls fail silently.

Then install JS plugin packages (run from project root, not src-tauri/):

```bash
pnpm add @tauri-apps/plugin-store @tauri-apps/plugin-global-shortcut @tauri-apps/plugin-autostart
```

These provide TypeScript bindings for the Rust plugins. Required now so later phases can import them without package.json changes.
  </action>
  <verify>
    <automated>cargo check --manifest-path "D:/develop/projects/riftle/.claude/worktrees/intelligent-burnell/src-tauri/Cargo.toml" 2>&1 | tail -5</automated>
  </verify>
  <done>tauri.conf.json has two windows with correct camelCase flags. capabilities/default.json has ["launcher", "settings"] in windows array and all five permission entries. cargo check still passes. pnpm-lock.yaml updated with three new plugin packages.</done>
</task>

<task type="auto">
  <name>Task 2: Replace App.vue with minimal transparent launcher shell</name>
  <files>src/App.vue</files>
  <action>
Replace src/App.vue with a minimal shell that:
1. Has no imports of the greet command or any Tauri API (prevents runtime errors on load)
2. Sets body background to transparent (critical — without this the launcher window shows a white rectangle despite transparent: true in tauri.conf.json)
3. Renders a minimal div structure that Phase 5 will replace with the full launcher UI

```vue
<script setup lang="ts">
// Phase 1: minimal launcher shell
// Full launcher UI implemented in Phase 5 (Launcher Window UI)
</script>

<template>
  <div id="app">
    <!-- Launcher UI placeholder — implemented in Phase 5 -->
  </div>
</template>

<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  /* Required for transparent window: without this, the launcher shows a white rectangle */
  background: transparent;
  overflow: hidden;
}

#app {
  width: 100%;
  height: 100%;
}
</style>
```

This file is intentionally minimal. Do not add search input, result list, or any launcher logic — that is Phase 5 scope.
  </action>
  <verify>
    <automated>cargo check --manifest-path "D:/develop/projects/riftle/.claude/worktrees/intelligent-burnell/src-tauri/Cargo.toml" 2>&1 | tail -3</automated>
  </verify>
  <done>App.vue contains no greet references, no Tauri API imports, and includes body { background: transparent } in the global style block. File has no TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke-test pnpm tauri dev and verify window flags</name>
  <files>src-tauri/tauri.conf.json</files>
  <action>Run `pnpm tauri dev` and verify the app starts cleanly. Temporarily set launcher visible:true to confirm window flags (frameless, shadow, transparent background, always-on-top, no taskbar entry). Revert to visible:false after verifying.</action>
  <verify>
    <automated>MISSING — pnpm tauri dev is a manual smoke test requiring a Windows display; no automated equivalent</automated>
  </verify>
  <done>pnpm tauri dev compiles and starts without errors. Launcher window flags confirmed correct when temporarily made visible. Settings window stays hidden.</done>
  <what-built>
Complete Phase 1 scaffold:
- Cargo.toml with all 12 crates (4 plugins + 7 domain crates)
- lib.rs with all plugins registered, empty invoke_handler, seven mod declarations
- Seven Rust module stubs (db, store, hotkey, indexer, search, commands, system_commands)
- tauri.conf.json with launcher window (frameless/transparent/always-on-top/hidden) and settings window (normal/hidden)
- capabilities/default.json covering both window labels with all plugin permissions
- App.vue minimal transparent shell (no greet, body transparent)
- JS plugin packages installed (@tauri-apps/plugin-store, global-shortcut, autostart)
  </what-built>
  <how-to-verify>
Run from the project root directory:

```
pnpm tauri dev
```

Expected behavior (first run takes ~60s for Rust compilation):
1. Terminal shows "Compiling riftle..." then "Finished" with no error lines
2. The launcher window does NOT appear (visible: false at launch)
3. No settings window appears
4. No errors in the WebView dev console
5. Terminal shows Vite started on http://localhost:1420

To verify launcher window flags, temporarily edit tauri.conf.json — change the launcher "visible": false to "visible": true — restart pnpm tauri dev and confirm:
- Window is frameless (no title bar or close/minimize buttons)
- Window has system drop shadow
- Window background is transparent (shows desktop through it) or the app div visible without white fill
- Always-on-top: open another window; launcher stays in front
- No taskbar entry for the launcher window

Revert visible back to false after verifying.

Common error signals and fixes:
- "error[E]: failed to resolve" — Cargo.toml crate version not found; check exact version strings
- Permission denied in dev console IPC calls — capabilities/default.json still has ["main"]; must be ["launcher", "settings"]
- Solid white rectangle instead of transparent — App.vue missing body { background: transparent }
  </how-to-verify>
  <resume-signal>Type "approved" if pnpm tauri dev starts cleanly and window flags look correct, or describe any issues seen.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete when:
1. `cargo check --manifest-path src-tauri/Cargo.toml` passes with no errors
2. `pnpm tauri dev` starts without compile or runtime errors
3. Launcher window hidden at start (visible: false)
4. Settings window hidden at start (visible: false)
5. No greet command or scaffold code remains in src-tauri/src/ or src/
6. Cargo.lock is committed to git (not untracked)
</verification>

<success_criteria>
- SCAF-02: tauri.conf.json declares launcher (frameless, skipTaskbar, alwaysOnTop, transparent, shadow, visible:false) and settings (normal, minWidth:600, minHeight:400, visible:false) with correct label and identifier
- SCAF-04: `pnpm tauri dev` compiles and starts without errors; no build warnings from missing features or version conflicts
- All Phase 1 files committed; Cargo.lock tracked by git
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-configuration/01-02-SUMMARY.md` using the summary template.
</output>
