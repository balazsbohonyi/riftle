---
phase: 09.3-asset-protocol-security-hardening
verified: 2026-03-10T21:35:00Z
status: human_needed
score: 4/5 must-haves verified
re_verification: false
---

# Phase 09.3: Asset Protocol Security Hardening - Verification Report

**Phase Goal:** Constrain `assetProtocol.scope` from `["**"]` to the app-owned icon directories and add server-side icon filename validation in Rust so path traversal or malformed icon filenames cannot reach the frontend.
**Verified:** 2026-03-10T21:35:00Z
**Status:** human_needed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `assetProtocol.scope` is no longer wildcarded and is restricted to installed and portable icon directories | VERIFIED | `src-tauri/tauri.conf.json` now contains `$DATA/riftle-launcher/icons/**` and `$EXE/../data/icons/**` only |
| 2 | `validate_icon_filename()` rejects traversal strings, absolute paths, wrong extensions, uppercase hex, and wrong lengths | VERIFIED | 8 `test_validate_icon_*` tests in `src-tauri/src/search.rs` all pass |
| 3 | `validate_icon_filename()` accepts 16-char lowercase hex PNG names plus `generic.png` and `system_command.png` | VERIFIED | `cargo test test_validate_icon` output: 8 passed, 0 failed |
| 4 | `score_and_rank()` substitutes `generic.png` when an app record contains an invalid icon filename | VERIFIED | `src-tauri/src/search.rs` wraps `app.icon_path` with `validate_icon_filename(&raw)` and falls back to `generic.png` |
| 5 | Icons still render in the launcher after the scope change | HUMAN NEEDED | Requires runtime smoke test in installed and portable modes |

**Score:** 4/5 truths verified automatically

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src-tauri/src/search.rs` | `validate_icon_filename()` implementation + call site in `score_and_rank()` + 8 tests | VERIFIED | Function definition and call site present; tests pass |
| `src-tauri/tauri.conf.json` | Hardened `assetProtocol.scope` | VERIFIED | Wildcard scope removed; two explicit icon globs present |
| Automated test output | New validator tests plus full suite remain green | VERIFIED | `cargo test test_validate_icon` and `cargo test` both exit 0 |

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| APSH-01 | Constrain asset protocol scope to app-owned icon directories | SATISFIED (code) / HUMAN NEEDED (runtime smoke) | Config updated; smoke test still required |
| APSH-02 | Validate icon filenames server-side | SATISFIED | `validate_icon_filename()` implemented and exercised by tests |
| APSH-03 | Automated tests cover accepted and rejected icon names | SATISFIED | 8 validator tests GREEN |

### Human Verification Required

1. Run `pnpm tauri dev`, search for several apps, and confirm icons render normally in installed/dev mode.
2. Repeat with the portable marker flow and confirm icons still render there as well.

If both checks pass, this phase can be marked complete. If either fails, the likely issue is the portable/install asset scope path resolution rather than the Rust validator.

### Automated Evidence

- `cargo test test_validate_icon` -> 8 passed, 0 failed
- `cargo test` -> 51 passed, 0 failed, 2 ignored

---
_Verified: 2026-03-10T21:35:00Z_
_Verifier: Codex direct execution_
