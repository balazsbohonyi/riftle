---
status: complete
phase: 09.3-asset-protocol-security-hardening
source:
  - 09.3-01-SUMMARY.md
  - 09.3-02-SUMMARY.md
started: 2026-03-11T00:11:27.2654519+02:00
updated: 2026-03-11T02:02:44.2006787+02:00
---

## Current Test

[testing complete]

## Tests

### 1. Installed/Dev Icon Rendering
expected: Start Riftle in the normal development or installed-style flow, open the launcher, and search for several apps with custom icons. The results list should render icons normally with no blank thumbnails or asset loading failures after the icon-loading change.
result: pass

### 2. Portable Mode Icon Rendering
expected: Run Riftle with the portable marker flow so icon assets resolve from the portable data directory, then search for several apps. Icons should render correctly without relying on the narrowed `$EXE/../data/icons/**` asset scope.
result: pass

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0

## Gaps

- truth: "Portable-mode search results render icons correctly without relying on unsupported Windows asset scope resolution."
  status: resolved
  reason: "Resolved in code by routing app-managed icon reads through the validated Rust get_icon_bytes command instead of the unsupported Windows `$EXE` asset scope."
  severity: major
  test: 2
  root_cause: "Phase 09.3 hardened assetProtocol.scope to use a portable `$EXE`-based branch, but Tauri 2.10.3 resolves `$EXE` via executable_dir(), which is not a reliable Windows path anchor for this case. The narrowed scope therefore did not authorize portable `<exe_dir>\\data\\icons\\**` paths even though Riftle still writes icons there and the frontend requested them correctly."
  artifacts:
    - path: "src-tauri/src/commands.rs"
      issue: "New validated get_icon_bytes path now owns app-managed icon reads under {data_dir}/icons."
    - path: "src/App.vue"
      issue: "Frontend now consumes cached blob URLs from the Rust command instead of convertFileSrc(data_dir/icons/<filename>)."
    - path: "src-tauri/tauri.conf.json"
      issue: "assetProtocol scope was removed because app-managed icon loading no longer depends on direct asset:// filesystem authorization."
  fix: "App.vue requests icon bytes from Rust, commands.rs validates filenames before reading from {data_dir}/icons, and tauri.conf.json no longer relies on assetProtocol scope for app-managed icons."
  debug_session: ".planning/debug/resolved/portable-mode-icon-rendering-fai.md"
