---
phase: 09.3
slug: asset-protocol-security-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-10
---

# Phase 09.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in (`cargo test`) |
| **Config file** | none — `Cargo.toml` `[dev-dependencies]` |
| **Quick run command** | `cd src-tauri && cargo test test_validate_icon` |
| **Full suite command** | `cd src-tauri && cargo test` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd src-tauri && cargo test`
- **After every plan wave:** Run `cd src-tauri && cargo test`
- **Before `/gsd:verify-work`:** Full suite green + manual icon-load smoke test approved
- **Max feedback latency:** ~5 seconds (unit tests); manual smoke required for APSH-01

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 09.3-01-01 | 01 | 0 | APSH-02, APSH-03 | unit | `cd src-tauri && cargo test test_validate_icon` | ❌ W0 | ⬜ pending |
| 09.3-02-01 | 02 | 1 | APSH-01 | manual smoke | Launch app, confirm icons load | N/A — manual | ⬜ pending |
| 09.3-02-02 | 02 | 1 | APSH-02, APSH-03 | unit | `cd src-tauri && cargo test test_validate_icon` | ❌ W0 | ⬜ pending |
| 09.3-02-03 | 02 | 1 | APSH-02 | unit | `cd src-tauri && cargo test` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `validate_icon_filename()` function stub in `src-tauri/src/search.rs`
- [ ] 8 test stubs in `src-tauri/src/search.rs` `#[cfg(test)] mod tests`:
  - `test_validate_icon_rejects_path_traversal` — `../etc/passwd.png` → invalid
  - `test_validate_icon_rejects_absolute_path` — `/etc/passwd.png` → invalid
  - `test_validate_icon_rejects_wrong_extension` — `abc.jpg` → invalid
  - `test_validate_icon_rejects_uppercase_hex` — `ABCDEF0123456789.png` → invalid (hex must be lowercase)
  - `test_validate_icon_rejects_wrong_length` — `abc.png` (too short) → invalid
  - `test_validate_icon_accepts_valid_hex` — `0123456789abcdef.png` → valid
  - `test_validate_icon_accepts_generic` — `generic.png` → valid
  - `test_validate_icon_accepts_system_command` — `system_command.png` → valid

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Icons load correctly after scope constraint (installed mode) | APSH-01 | `tauri.conf.json` scope is not unit-testable | Run `pnpm tauri dev`, search for apps, confirm icons display |
| Icons load correctly after scope constraint (portable mode) | APSH-01 | Requires `$EXE/../data/icons/**` pattern to resolve correctly in Tauri scope engine | Place `riftle-launcher.portable` next to exe, run app, confirm icons display |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
