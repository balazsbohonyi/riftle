---
phase: 09.3-asset-protocol-security-hardening
plan: 03
type: gap_closure
wave: 3
depends_on:
  - 09.3-02
files_modified:
  - src/App.vue
  - src-tauri/src/lib.rs
  - src-tauri/src/commands.rs
  - src-tauri/tauri.conf.json
  - src-tauri/src/search.rs
autonomous: true
requirements:
  - APSH-01
  - APSH-02
  - APSH-03
  - GAP-UAT-09.3-01

must_haves:
  truths:
    - "Portable-mode icons render correctly when Riftle runs with the portable marker present"
    - "Installed/dev mode icons continue to render correctly after the portable fix"
    - "Icon loading no longer depends on a brittle `$EXE`-based asset scope for app-managed files"
    - "Server-side icon filename validation remains enforced for any icon file request"
    - "No wildcard filesystem asset scope is reintroduced"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "Rust command that returns validated icon bytes or a data URL from `{data_dir}/icons/<filename>`"
      contains: "icon"
    - path: "src/App.vue"
      provides: "frontend icon loading path that consumes the Rust command instead of direct `convertFileSrc()` for app-managed icons"
      contains: "getIconUrl"
    - path: "src-tauri/tauri.conf.json"
      provides: "security configuration without fragile portable `$EXE` dependence"
      contains: "assetProtocol"
  key_links:
    - from: "src-tauri/src/search.rs"
      to: "src-tauri/src/commands.rs"
      via: "validate_icon_filename() reused before any icon file read"
      pattern: "validate_icon_filename"
    - from: "src-tauri/src/store.rs"
      to: "src/App.vue"
      via: "data_dir informs icon lookup root"
      pattern: "data_dir"
---

<objective>
Close the Phase 09.3 UAT gap where portable-mode icons break under the hardened asset scope.

Purpose: preserve the Phase 09.3 security hardening while making icon loading portable-safe on Windows without relying on static `$EXE` scope resolution.
Output: validated runtime icon loading through Rust, portable and installed icon smoke tests green, wildcard asset access still absent.
</objective>

<context>
@.planning/phases/09.3-asset-protocol-security-hardening/09.3-UAT.md
@.planning/debug/portable-mode-icon-rendering-fai.md
@src-tauri/tauri.conf.json
@src-tauri/src/paths.rs
@src-tauri/src/store.rs
@src-tauri/src/search.rs
@src/App.vue
@src-tauri/src/lib.rs
@src-tauri/src/commands.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a validated Rust icon-read command for app-managed icons</name>
  <files>src-tauri/src/commands.rs, src-tauri/src/lib.rs, src-tauri/src/search.rs</files>
  <action>
    Add a small Tauri command that accepts an icon filename, validates it with the existing `validate_icon_filename()` allowlist, resolves the file under `{data_dir}/icons/`, and returns either a `data:` URL or raw bytes that the frontend can bind directly to `<img src>`.

    Guardrails:
    - Reject any filename that fails `validate_icon_filename()`
    - Never allow arbitrary paths or traversal
    - Return a clean fallback for missing files so the UI can show `generic.png` instead of a broken image
    - Keep the implementation scoped to icon loading only; do not generalize into an unrestricted file-read API

    Wire the command into `lib.rs` invoke registration.
  </action>
  <verify>
    <automated>Add Rust tests for the helper logic: valid filename resolves under `data_dir/icons`, traversal is rejected, missing files fail cleanly, and generic fallback remains reachable.</automated>
    <automated>`cd D:/develop/projects/riftle/src-tauri && cargo test` exits 0.</automated>
  </verify>
  <done>
    - A Rust-side icon read path exists and is guarded by `validate_icon_filename()`
    - The helper has automated coverage for valid, invalid, and missing icon cases
    - `cargo test` remains green
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the launcher to use the Rust icon command with a small cache</name>
  <files>src/App.vue</files>
  <action>
    Replace direct `convertFileSrc(<data_dir>/icons/<filename>)` loading for app-managed icons with an async lookup that calls the new Rust command and caches results by filename so repeated search results do not refetch the same icon data.

    Requirements:
    - Preserve existing behavior for normal rendering and search responsiveness
    - Avoid broken-image UI when an icon read fails; fall back cleanly to the generic icon data
    - Keep the change local to icon loading; do not disturb keyboard/search behavior
  </action>
  <verify>
    <manual>Run Riftle in normal dev mode and confirm icons still render while searching several apps.</manual>
    <manual>Run Riftle in portable mode and confirm the same search results render icons correctly.</manual>
  </verify>
  <done>
    - App.vue no longer depends on direct filesystem asset URLs for app-managed icons
    - Repeated icon lookups are cached
    - Installed and portable icon smoke tests both pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Simplify security config after removing portable asset dependency</name>
  <files>src-tauri/tauri.conf.json</files>
  <action>
    Remove or simplify the fragile portable `$EXE`-based asset scope entry once app-managed icons no longer require direct `asset://` reads from the portable data directory.

    Keep the overall security posture at least as strict as Phase 09.3:
    - do not reintroduce `"**"`
    - keep only the scopes still required by the final loading strategy
    - document in the summary that portable icon reads now flow through the validated Rust command instead of Tauri's static filesystem scope
  </action>
  <verify>
    <automated>Confirm `src-tauri/tauri.conf.json` contains no wildcard `"**"` scope entry.</automated>
    <manual>Repeat both installed/dev and portable-mode icon smoke tests after the config cleanup.</manual>
  </verify>
  <done>
    - Security config matches the final icon-loading design
    - No brittle portable `$EXE` dependency remains
    - Both UAT smoke tests remain green after cleanup
  </done>
</task>

</tasks>

<verification>
1. `cargo test` exits 0 with new helper tests green.
2. Installed/dev icon rendering smoke test passes.
3. Portable-mode icon rendering smoke test passes.
4. Missing icon files fall back cleanly instead of showing broken image UI.
5. `src-tauri/tauri.conf.json` still contains no `"**"` asset scope.
6. Portable icon loading path is now explained by runtime Rust validation, not by `$EXE` scope guessing.
</verification>

<success_criteria>
- Portable-mode icons no longer break under the hardened security model.
- The fix preserves or improves the 09.3 security posture.
- Installed/dev mode remains green.
- The resulting plan is executable by `$gsd-execute-phase 09.3 --gaps-only` without needing another design round.
</success_criteria>
