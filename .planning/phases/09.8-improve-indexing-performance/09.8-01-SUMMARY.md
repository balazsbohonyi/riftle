---
phase: 09.8-improve-indexing-performance
plan: "01"
subsystem: database
tags: [rusqlite, sqlite, tdd]

# Dependency graph
requires:
  - phase: 02-data-layer
    provides: db.rs AppRecord, init_db_connection, upsert_app, Connection pattern
provides:
  - pub fn count_apps(conn: &Connection) -> rusqlite::Result<i64> in db.rs
affects: [09.8-02, lib.rs startup sequence]

# Tech tracking
tech-stack:
  added: []
  patterns: [scalar query_row with conn parameter, TDD RED-GREEN cycle with in_memory_db helper]

key-files:
  created: []
  modified: [src-tauri/src/db.rs]

key-decisions:
  - "count_apps placed after increment_launch_count and before #[cfg(test)] block — consistent with existing function ordering in db.rs"
  - "dead_code lint warning on count_apps is expected and intentional — function will be called from lib.rs in Plan 02"

patterns-established:
  - "scalar COUNT(*) query uses conn.query_row with empty params slice [] and |row| row.get(0) closure"

requirements-completed: [09.8-D4]

# Metrics
duration: 2min
completed: 2026-05-16
---

# Phase 09.8 Plan 01: count_apps DB Function Summary

**`count_apps(conn: &Connection) -> rusqlite::Result<i64>` added to db.rs via TDD with two unit tests verifying empty-DB and post-insert counts**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-16T08:21:55Z
- **Completed:** 2026-05-16T08:23:55Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `pub fn count_apps` using `query_row("SELECT COUNT(*) FROM apps")` pattern matching existing db.rs scalar queries
- Added `test_count_apps_empty` — verifies `Ok(0)` on a freshly initialised in-memory DB
- Added `test_count_apps_returns_correct_count` — verifies `Ok(2)` after two `upsert_app` calls
- Full Rust test suite remains green: 104 passed, 0 failed, 2 ignored

## Task Commits

Each task was committed atomically:

1. **Task 1: Add count_apps function and unit tests to db.rs** - `a810bd3` (feat)

**Plan metadata:** (docs commit follows)

_Note: TDD RED state confirmed via compile error before GREEN implementation._

## Files Created/Modified
- `src-tauri/src/db.rs` — added `count_apps` public function and two unit tests

## Decisions Made
- `count_apps` positioned between `increment_launch_count` and the `#[cfg(test)]` block to maintain the existing top-to-bottom function ordering
- `dead_code` lint on the new function is expected — it becomes used in Plan 02 when `lib.rs` calls it at startup

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `count_apps` is exported from `db.rs` and ready to be called by `lib.rs` in Plan 02
- Plan 02 can now implement deferred-startup-index logic by comparing `count_apps` result to 0
- No blockers

---
*Phase: 09.8-improve-indexing-performance*
*Completed: 2026-05-16*
