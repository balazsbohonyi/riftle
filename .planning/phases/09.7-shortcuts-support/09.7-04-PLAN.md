---
phase: 09.7-shortcuts-support
plan: 04
type: execute
wave: 3
depends_on:
  - 09.7-01
  - 09.7-02
  - 09.7-03
files_modified:
  - src/components/ui/ShortcutList.vue
  - src/Settings.vue
  - src/App.vue
autonomous: false
requirements:
  - 09.7-UI-01
  - 09.7-TBD-01
  - 09.7-TBD-03
  - 09.7-TBD-04
  - 09.7-TBD-05
  - 09.7-TBD-08
must_haves:
  truths:
    - "User can add, edit, save, and reopen directory shortcuts in Settings"
    - "User can add, edit, save, and reopen file shortcuts with optional executable parameters"
    - "Shortcut results launch through the new shortcut IPC path and show warnings without hiding on failure"
  artifacts:
    - path: "src/components/ui/ShortcutList.vue"
      provides: "Compact directory/file shortcut editor component"
      exports: ["default"]
    - path: "src/Settings.vue"
      provides: "Shortcuts section below Search with validation and persistence"
      contains: "Shortcuts"
    - path: "src/App.vue"
      provides: "Shortcut result launch routing and warning handling"
      contains: "launch_shortcut"
  key_links:
    - from: "src/Settings.vue"
      to: "set_settings_cmd"
      via: "full Settings struct round-trip including shortcut arrays"
      pattern: "directory_shortcuts|file_shortcuts"
    - from: "src/App.vue"
      to: "launch_shortcut"
      via: "kind-based dispatch for shortcut_dir and shortcut_file"
      pattern: "shortcut_dir|shortcut_file"
---

<objective>
Wire shortcuts into the user experience.

Purpose: The backend contract, search results, and launch command only matter once users can configure shortcuts in Settings and launch them from the keyboard-first launcher.
Output: A new Shortcuts settings section, a reusable editor component, and App.vue routing for shortcut results and failure warnings.
</objective>

<execution_context>
@C:/Users/Balazs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.7-shortcuts-support/09.7-CONTEXT.md
@.planning/phases/09.7-shortcuts-support/09.7-RESEARCH.md
@.planning/phases/09.7-shortcuts-support/09.7-VALIDATION.md
@.planning/phases/09.7-shortcuts-support/09.7-01-SUMMARY.md
@.planning/phases/09.7-shortcuts-support/09.7-02-SUMMARY.md
@.planning/phases/09.7-shortcuts-support/09.7-03-SUMMARY.md
@.planning/phases/08-settings-window/08-03-SUMMARY.md
@.planning/phases/09.3-asset-protocol-security-hardening/09.3-03-SUMMARY.md
@.planning/phases/09.5-backend-resilience/09.5-01-SUMMARY.md
@graphify-out/GRAPH_REPORT.md
@src/Settings.vue
@src/App.vue
@src/components/ui/PathList.vue

<interfaces>
Settings payload additions:
```ts
interface DirectoryShortcut {
  path: string
  alias: string
}

interface FileShortcut {
  path: string
  parameters: string
  alias: string
}

interface SettingsData {
  directory_shortcuts: DirectoryShortcut[]
  file_shortcuts: FileShortcut[]
}
```

Shortcut launch result:
```ts
interface ShortcutLaunchResult {
  success: boolean
  warning?: BackendWarning | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shortcut editor component</name>
  <files>src/components/ui/ShortcutList.vue</files>
  <action>Create `ShortcutList.vue` as a richer sibling to `PathList.vue`. It should support two modes: `directory` and `file`. Directory entries show path plus alias. File entries show path, parameters, then alias as the final field. Use dynamic import of `@tauri-apps/plugin-dialog`; folder mode calls `open({ directory: true, multiple: false })`, file mode calls `open({ multiple: false })`. Emit `update:modelValue` and `change` with the full edited array. Keep layout compact and token-based like existing Settings controls. Do not add a structured argument builder.</action>
  <verify>
    <automated>pnpm.cmd build</automated>
  </verify>
  <done>Component compiles, supports add/remove/edit for both shortcut types, and follows existing Settings control styling.</done>
</task>

<task type="auto">
  <name>Task 2: Add Shortcuts section and UI validation in Settings</name>
  <files>src/Settings.vue, src/components/ui/ShortcutList.vue</files>
  <action>Extend `SettingsData` and `SettingsResponse` with `directory_shortcuts` and `file_shortcuts`. Populate them on mount and include them in `saveSettings()`. Add a `Shortcuts` section directly below `Search` with `Directories` and `Files` subsections using `ShortcutList.vue`. Add frontend validation before save for duplicate aliases/fallback names case-insensitively, missing alias for parameterized executable/app shortcuts, and parameters on non-executable/non-app files. Render validation errors inline in the section and avoid calling `set_settings_cmd` while invalid. Preserve the backend validation as final authority.</action>
  <verify>
    <automated>pnpm.cmd build</automated>
  </verify>
  <done>Users can configure shortcuts in Settings, invalid entries are rejected before save, and the settings payload round-trips both arrays.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wire launcher shortcut launch routing and perform runtime smoke</name>
  <files>src/App.vue</files>
  <action>Update `SearchResult` launch routing so `kind === "shortcut_dir"` or `kind === "shortcut_file"` invokes `launch_shortcut`. Hide the launcher only when the returned `success` is true. If the result includes a warning, append it to the existing backend warning banner without hiding the launcher. Keep `launch_elevated` behavior unchanged and do not route shortcut results through elevated launch.</action>
  <verify>
    <automated>pnpm.cmd build</automated>
  </verify>
  <what-built>Shortcuts Settings UI plus launcher routing for shortcut search results and warning display.</what-built>
  <how-to-verify>
    1. Run `pnpm tauri dev`.
    2. Open Settings and add one directory shortcut with an alias; save, close, reopen Settings, and confirm it persists.
    3. Add one document file shortcut without parameters and one executable shortcut with raw parameters and a non-empty alias.
    4. Search each alias in the launcher; confirm shortcut results appear above app results.
    5. Press Enter on the directory shortcut; confirm the folder opens and launcher hides.
    6. Press Enter on the document shortcut; confirm the default app opens or Windows Open With appears when there is no association.
    7. Add or edit a shortcut to a missing path, launch it, and confirm the warning appears while the launcher remains visible.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <done>Runtime smoke verifies picker persistence, ordering, successful launch hide behavior, and failed launch warning behavior.</done>
</task>

</tasks>

<verification>
Before human verification:
```sh
cd src-tauri && cargo test shortcut_
cd src-tauri && cargo test
pnpm.cmd build
graphify update .
```
</verification>

<success_criteria>
- Shortcuts section appears below Search with Directories and Files subsections.
- Directory and file shortcut payloads persist through settings reload.
- Launcher dispatches shortcut results to `launch_shortcut`.
- Failed launches keep the launcher visible and show a lightweight warning.
- `graphify update .` has been run after code modifications, per project instruction.
</success_criteria>

<output>
After completion, create `.planning/phases/09.7-shortcuts-support/09.7-04-SUMMARY.md`
</output>
