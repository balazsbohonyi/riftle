---
phase: 09.7-shortcuts-support
plan: 02
type: execute
wave: 2
depends_on:
  - 09.7-01
files_modified:
  - src-tauri/src/search.rs
autonomous: true
requirements:
  - 09.7-TBD-06
  - 09.7-TBD-07
must_haves:
  truths:
    - "Typing a shortcut alias or fallback name returns matching shortcuts above normal apps"
    - "Shortcut search is prefix-first and capped with the existing 50-result limit"
    - "Directory and file shortcuts display sensible fallback names when alias is empty"
  artifacts:
    - path: "src-tauri/src/search.rs"
      provides: "Shortcut result generation and merge before app search results"
      exports: ["search"]
    - path: "src-tauri/src/shortcuts.rs"
      provides: "Shortcut display names and IDs consumed by search"
      exports: ["shortcut_display_name", "shortcut_id"]
  key_links:
    - from: "src-tauri/src/search.rs"
      to: "src-tauri/src/store.rs"
      via: "search command loads current settings"
      pattern: "load_settings_outcome|get_settings"
    - from: "shortcut results"
      to: "SearchResult contract"
      via: "kind values route frontend launch behavior"
      pattern: "shortcut_dir|shortcut_file"
    - from: "shortcut results"
      to: "existing icon filename validation"
      via: "app-owned generic.png filename reused for directory and file shortcuts"
      pattern: "generic\\.png"
---

<objective>
Make configured shortcuts first-class search results.

Purpose: The launcher must show user shortcuts through the existing `search` IPC contract while preserving system-command routing, app fuzzy ranking, and the 50-result cap.
Output: Shortcut matching and result merging in `search.rs`, with tests for ordering, fallback names, and cap behavior.
</objective>

<execution_context>
@C:/Users/Balazs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.7-shortcuts-support/09.7-CONTEXT.md
@.planning/phases/09.7-shortcuts-support/09.7-RESEARCH.md
@.planning/phases/09.7-shortcuts-support/09.7-VALIDATION.md
@.planning/phases/09.7-shortcuts-support/09.7-01-SUMMARY.md
@graphify-out/GRAPH_REPORT.md
@src-tauri/src/search.rs
@src-tauri/src/store.rs
@src-tauri/src/shortcuts.rs

<interfaces>
Existing search contract:
```rust
pub struct SearchResult {
    pub id: String,
    pub name: String,
    pub icon_path: String,
    pub path: String,
    pub kind: String,
    pub requires_elevation: bool,
}

pub fn score_and_rank(query: &str, apps: &[AppRecord]) -> Vec<SearchResult>;
#[tauri::command]
pub fn search(query: String, index_state: tauri::State<SearchIndexState>) -> Vec<SearchResult>;
```

Expected shortcut result kinds:
```text
shortcut_dir
shortcut_file
```

Expected shortcut icon filename:
```text
generic.png
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shortcut prefix matching and fallback display tests</name>
  <files>src-tauri/src/search.rs</files>
  <behavior>
    - Test 1: alias prefix matches a directory shortcut.
    - Test 2: empty directory alias displays and matches the directory basename.
    - Test 3: empty file alias displays and matches the filename without extension.
  </behavior>
  <action>Add pure helper coverage in `search.rs` around a new shortcut search helper that accepts a query plus `Settings` or shortcut slices. Prefix matching should be case-insensitive and use alias first, then fallback name when alias is empty. Directory shortcut rows use `kind: "shortcut_dir"` and file shortcut rows use `kind: "shortcut_file"`. Use the existing app-owned `generic.png` icon filename for both directory and file shortcut rows in this phase; do not introduce user-controlled icon paths or new shortcut icon assets. Keep returned icon filenames compatible with the existing icon filename validation. Do not add fuzzy matching for shortcuts.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml shortcut_fallback_names</automated>
  </verify>
  <done>Shortcut rows have stable IDs, correct kind values, expected display names, target paths in `path`, and `icon_path: "generic.png"`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Merge shortcuts above app results in search command</name>
  <files>src-tauri/src/search.rs</files>
  <behavior>
    - Test 1: a shortcut and an app matching the same query returns the shortcut first.
    - Test 2: shortcut matches consume part of the 50-result cap and app results fill the remaining slots.
    - Test 3: `>` system command searches still return only system commands and no shortcut rows.
  </behavior>
  <action>Update `search()` so the system-command branch remains first. For normal queries, read current settings from the managed data directory or another existing settings access path, compute shortcut matches, then append `score_and_rank()` app results truncated to `50 - shortcuts.len()`. Keep app search behavior unchanged when there are no shortcut matches. If settings cannot be read, log and continue with app-only search instead of failing the command.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml shortcut_search_precedes_apps</automated>
  </verify>
  <done>Matching shortcuts always precede app results, the combined response is capped at 50, and system-command search remains isolated.</done>
</task>

</tasks>

<verification>
Run:
```sh
cargo test --manifest-path src-tauri/Cargo.toml shortcut_
cargo test --manifest-path src-tauri/Cargo.toml search
```
</verification>

<success_criteria>
- Shortcut search honors locked prefix-first behavior.
- Search result contract remains compatible with `App.vue`.
- Normal app fuzzy ranking is unchanged behind shortcut matches.
- Deferred fuzzy shortcut behavior is not introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/09.7-shortcuts-support/09.7-02-SUMMARY.md`
</output>
