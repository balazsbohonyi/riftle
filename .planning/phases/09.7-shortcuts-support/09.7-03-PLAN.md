---
phase: 09.7-shortcuts-support
plan: 03
type: execute
wave: 2
depends_on:
  - 09.7-01
files_modified:
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - 09.7-TBD-08
  - 09.7-TBD-09
must_haves:
  truths:
    - "Launching an existing directory shortcut opens the folder through Windows shell handling"
    - "Launching an existing file shortcut opens through the default application"
    - "Missing or failed shortcut launches report failure without forcing the launcher to hide"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "launch_shortcut command and Windows shell launch policy helpers"
      exports: ["launch_shortcut"]
    - path: "src-tauri/src/lib.rs"
      provides: "launch_shortcut IPC registration"
      contains: "commands::launch_shortcut"
  key_links:
    - from: "src-tauri/src/commands.rs"
      to: "src-tauri/src/store.rs"
      via: "launch resolves shortcut ID against current settings"
      pattern: "load_settings_outcome|get_settings"
    - from: "launch_shortcut"
      to: "App.vue"
      via: "returns structured success/failure for frontend hide decision"
      pattern: "ShortcutLaunchResult|success"
---

<objective>
Add Rust-owned shortcut launching.

Purpose: Shortcuts must use Windows shell semantics while preserving the existing boundary: Vue routes user intent, Rust owns OS interaction and error classification.
Output: `launch_shortcut` IPC command plus testable policy helpers for missing targets and Open With fallback.
</objective>

<execution_context>
@C:/Users/Balazs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.7-shortcuts-support/09.7-CONTEXT.md
@.planning/phases/09.7-shortcuts-support/09.7-RESEARCH.md
@.planning/phases/09.7-shortcuts-support/09.7-VALIDATION.md
@.planning/phases/09.7-shortcuts-support/09.7-01-SUMMARY.md
@.planning/phases/06-launch-actions/06-01-SUMMARY.md
@.planning/phases/09.5-backend-resilience/09.5-01-SUMMARY.md
@graphify-out/GRAPH_REPORT.md
@src-tauri/src/commands.rs
@src-tauri/src/lib.rs
@src-tauri/src/shortcuts.rs

<interfaces>
Existing launch patterns:
```rust
#[tauri::command]
pub fn launch(id: String, app: tauri::AppHandle) -> Result<(), String>;

#[tauri::command]
pub fn launch_elevated(id: String, app: tauri::AppHandle) -> Result<(), String>;
```

New IPC return contract:
```rust
#[derive(Debug, Clone, Serialize)]
pub struct ShortcutLaunchResult {
    pub success: bool,
    pub warning: Option<crate::warnings::BackendWarning>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shortcut launch policy helpers and tests</name>
  <files>src-tauri/src/commands.rs</files>
  <behavior>
    - Test 1: missing shortcut target produces `success: false` policy result.
    - Test 2: parameters are passed only for parameter-capable executable targets.
    - Test 3: Open With fallback is attempted only for non-executable file results that fail with `SE_ERR_NOASSOC`.
  </behavior>
  <action>Add pure helpers in `commands.rs` for resolving a shortcut ID from current settings, classifying target kind, converting ShellExecute return values into success/failure/no-association decisions, and building a lightweight `BackendWarning` for failed launches. Keep these helpers unit-testable without invoking ShellExecute. Use the executable extension policy from `shortcuts.rs`; do not accept parameters for documents or `.lnk` in this phase.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml shortcut_</automated>
  </verify>
  <done>Policy tests prove missing targets fail visibly and Open With is only selected for non-executable no-association failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and register launch_shortcut IPC command</name>
  <files>src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Test 1: command helper returns success for shell success codes greater than 32.
    - Test 2: command helper returns a warning payload for missing targets or shell failure codes.
    - Test 3: launch command does not hide the launcher; frontend will hide only when `success` is true.
  </behavior>
  <action>Implement `#[tauri::command] pub fn launch_shortcut(id: String, app: tauri::AppHandle, data_dir: tauri::State<PathBuf>) -> Result<ShortcutLaunchResult, String>`. Resolve the current shortcut from settings by stable ID. For directories and non-parameterized files, call `ShellExecuteW` with default verb and null parameters. For executable/app file shortcuts, pass the raw parameters string as `lpParameters` exactly as stored. If ShellExecute returns `SE_ERR_NOASSOC` for a non-executable file, call `SHOpenWithDialog` if the current `windows` feature set supports it; otherwise return a warning instead of adding an undocumented fallback. Register the command in `lib.rs` invoke handler.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml shortcut_</automated>
  </verify>
  <done>`launch_shortcut` is callable by IPC, returns a structured success flag, and never hides the launcher from Rust on failure.</done>
</task>

</tasks>

<verification>
Run:
```sh
cargo test --manifest-path src-tauri/Cargo.toml shortcut_
cargo test --manifest-path src-tauri/Cargo.toml
```
</verification>

<success_criteria>
- Shortcut launching is Rust-owned and uses ShellExecuteW.
- Missing target or failed launch produces a visible warning payload for frontend rendering.
- Successful launch can be distinguished so the frontend can hide the launcher only on success.
- Raw arguments are only used for executable/app file shortcuts, per locked user decision.
</success_criteria>

<output>
After completion, create `.planning/phases/09.7-shortcuts-support/09.7-03-SUMMARY.md`
</output>
