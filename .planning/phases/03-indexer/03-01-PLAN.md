---
phase: 03-indexer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/indexer.rs
  - src-tauri/icons/generic.png
autonomous: true
requirements:
  - INDX-01
  - INDX-02
  - INDX-03
  - INDX-04
  - INDX-05
  - INDX-06
  - INDX-07
  - INDX-08

must_haves:
  truths:
    - "All new crates compile without errors (lnk, notify-debouncer-mini, image)"
    - "All 12 test stubs exist in indexer.rs and compile to failing tests (RED state)"
    - "generic.png exists at src-tauri/icons/generic.png at compile time for include_bytes!"
    - "All public function signatures are defined so later plans compile against them"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "lnk, notify-debouncer-mini, image crates + Win32_Graphics_Gdi + Win32_UI_WindowsAndMessaging features"
      contains: "lnk = \"^0.3\""
    - path: "src-tauri/src/indexer.rs"
      provides: "All public function stubs + test module with 12 failing test stubs"
      min_lines: 80
    - path: "src-tauri/icons/generic.png"
      provides: "32x32 PNG compiled into binary via include_bytes!"
  key_links:
    - from: "src-tauri/src/indexer.rs"
      to: "src-tauri/icons/generic.png"
      via: "include_bytes! macro at compile time"
      pattern: "include_bytes!"
---

<objective>
Wave 0 scaffold: add three new Cargo dependencies (lnk, notify-debouncer-mini, image), extend windows-sys features, create the generic.png placeholder, and write all public function signatures plus 12 failing test stubs in indexer.rs.

Purpose: Nyquist compliance — all later plans implement against defined contracts. Tests are written in RED state here so each implementing plan turns them GREEN.
Output: Compilable scaffold with failing tests. Plans 02-05 depend on these contracts being in place.
</objective>

<execution_context>
@C:/Users/Balazs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-indexer/03-CONTEXT.md
@.planning/phases/03-indexer/03-RESEARCH.md
@.planning/phases/03-indexer/03-VALIDATION.md
@src-tauri/Cargo.toml
@src-tauri/src/indexer.rs
@src-tauri/src/db.rs
@src-tauri/src/store.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Cargo.toml with Phase 3 dependencies</name>
  <files>src-tauri/Cargo.toml</files>
  <action>
    Add three new crates to the `[dependencies]` section of src-tauri/Cargo.toml, after the existing domain crates block:

    ```toml
    # Phase 3: Indexer dependencies
    lnk = "^0.3"
    notify-debouncer-mini = "0.4"
    image = { version = "^0.25", default-features = false, features = ["png"] }
    ```

    Update the windows-sys entry to add two new features:

    ```toml
    windows-sys = { version = "^0.52", features = [
      "Win32_UI_Shell",
      "Win32_System_Shutdown",
      "Win32_System_Power",
      "Win32_System_RemoteDesktop",
      "Win32_Graphics_Gdi",           # NEW: GetIconInfo, GetDIBits, DeleteObject, CreateCompatibleDC
      "Win32_UI_WindowsAndMessaging",  # NEW: ICONINFO, DestroyIcon
    ] }
    ```

    Do NOT add Win32_System_Com — the lnk crate approach avoids COM entirely per research decision.

    After editing, run `cargo check -p riftle` (NOT cargo build — slower) from src-tauri directory to confirm new crates resolve without compile errors.
  </action>
  <verify>
    <automated>cd /d/develop/projects/riftle/.claude/worktrees/eloquent-clarke/src-tauri && cargo check -p riftle 2>&1 | tail -5</automated>
  </verify>
  <done>cargo check succeeds; Cargo.lock updated with lnk, notify-debouncer-mini, image entries; windows-sys has 6 features listed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create generic.png and write indexer.rs scaffold with failing test stubs</name>
  <files>src-tauri/icons/generic.png, src-tauri/src/indexer.rs</files>
  <behavior>
    - test_crawl_discovers_exe: crawl_dir on a temp dir containing a foo.exe finds 1 app record with path pointing to foo.exe
    - test_crawl_discovers_lnk: crawl_dir on a temp dir containing a valid .lnk finds 1 app record (stub: just assert panics or todo!() — lnk creation is complex, skip for now; mark #[ignore] if needed)
    - test_resolve_lnk_valid: resolve_lnk on a real .lnk file (if accessible) returns Some(path) — stub panics with todo!() or is #[ignore]
    - test_resolve_lnk_broken: resolve_lnk on a non-existent path returns None
    - test_prune_stale: given a DB with 2 apps and discovered_ids containing only 1, after prune_stale the DB has 1 row
    - test_crawl_excludes_path: crawl_dir on a temp dir with an excluded path skips apps under that path
    - test_icon_filename_stable: icon_filename("C:\\foo\\bar.exe") called twice returns identical hex strings
    - test_generic_icon_bootstrap: ensure_generic_icon writes generic.png to a temp dir if missing; second call is a no-op (file size unchanged)
    - test_timer_fires: start_timer_thread with 100ms interval triggers at least one index call within 500ms
    - test_timer_reset: sending () to the timer Sender resets the deadline
    - test_atomic_guard_prevents_double_index: two concurrent try_start_index calls on the same Arc<AtomicBool> result in only one indexing run
  </behavior>
  <action>
    Step 1: Create src-tauri/icons/generic.png.
    The file already has Tauri icons at src-tauri/icons/ (32x32.png exists). Copy the existing 32x32.png as generic.png:
    - Use std::fs::copy or the Write tool to write a minimal valid PNG.
    - IMPORTANT: A real PNG must exist here — include_bytes! at compile time fails on an empty file.
    - Use a simple approach: copy src-tauri/icons/32x32.png as src-tauri/icons/generic.png.

    Step 2: Write src-tauri/src/indexer.rs with this complete scaffold:

    ```rust
    // Phase 3: Windows application indexer
    // crawl Start Menu, Desktop, PATH; .lnk resolution; icon extraction; background refresh

    use std::collections::HashSet;
    use std::path::{Path, PathBuf};
    use std::sync::{Arc, Mutex, mpsc};
    use std::sync::atomic::{AtomicBool, Ordering};
    use rusqlite::Connection;

    use crate::db::{AppRecord, upsert_app};
    use crate::store::Settings;

    // Generic icon bundled at compile time — path relative to this source file
    static GENERIC_ICON: &[u8] = include_bytes!("../../icons/generic.png");

    // ---- Public API (called from lib.rs) ----

    /// Run a full blocking index. Called synchronously in setup() before app is ready.
    /// Crawls all source dirs, upserts apps, extracts icons async, prunes stale entries.
    pub fn run_full_index(db: &Arc<Mutex<Connection>>, data_dir: &Path, settings: &Settings) {
        todo!("Phase 3 Plan 04: implement full index")
    }

    /// Spawn background timer thread and filesystem watcher thread.
    /// Called once after run_full_index() completes in setup().
    /// Returns the timer reset Sender — store as managed state for reindex() command.
    pub fn start_background_tasks(
        db: Arc<Mutex<Connection>>,
        data_dir: PathBuf,
        settings: &Settings,
        is_indexing: Arc<AtomicBool>,
    ) -> mpsc::Sender<()> {
        todo!("Phase 3 Plan 05: implement background tasks")
    }

    /// Tauri command: fire-and-forget manual re-index.
    /// Spawns a thread, returns immediately. Frontend shows loading state.
    #[tauri::command]
    pub fn reindex(
        db_state: tauri::State<crate::db::DbState>,
        is_indexing: tauri::State<Arc<AtomicBool>>,
        timer_tx: tauri::State<Arc<Mutex<mpsc::Sender<()>>>>,
        data_dir: tauri::State<PathBuf>,
    ) {
        todo!("Phase 3 Plan 05: implement reindex command")
    }

    // ---- Internal functions ----

    /// Get all directories to index, with their source labels.
    pub(crate) fn get_index_paths(settings: &Settings) -> Vec<(PathBuf, &'static str)> {
        todo!("Phase 3 Plan 02: implement path discovery")
    }

    /// Walk a directory, resolve .lnk shortcuts, return AppRecords.
    /// source: "start_menu" | "desktop" | "path" | "additional"
    /// PATH source: .exe only, no .lnk resolution, max_depth 1.
    pub(crate) fn crawl_dir(root: &Path, source: &'static str, excluded: &[String]) -> Vec<AppRecord> {
        todo!("Phase 3 Plan 02: implement crawl")
    }

    /// Resolve a .lnk file to its target executable path.
    /// Returns None if: unresolvable, target is another .lnk, target doesn't exist, target isn't .exe.
    pub(crate) fn resolve_lnk(lnk_path: &Path) -> Option<PathBuf> {
        todo!("Phase 3 Plan 02: implement LNK resolution")
    }

    /// Build a canonical AppRecord from an exe path.
    pub(crate) fn make_app_record(exe_path: &Path, source: &'static str) -> AppRecord {
        todo!("Phase 3 Plan 02: implement make_app_record")
    }

    /// FNV-1a 64-bit hash of the normalized exe path → "{:016x}.png" icon filename.
    pub(crate) fn icon_filename(exe_path: &str) -> String {
        todo!("Phase 3 Plan 02: implement icon_filename")
    }

    /// Delete all apps from DB whose id is not in discovered_ids.
    pub(crate) fn prune_stale(conn: &Connection, discovered_ids: &HashSet<String>) -> rusqlite::Result<()> {
        todo!("Phase 3 Plan 02: implement prune_stale")
    }

    /// Copy bundled GENERIC_ICON to {data_dir}/icons/generic.png if missing.
    pub(crate) fn ensure_generic_icon(data_dir: &Path) -> std::io::Result<()> {
        todo!("Phase 3 Plan 03: implement ensure_generic_icon")
    }

    /// Extract a 32x32 RGBA PNG from an exe file using Windows GDI.
    /// Returns PNG bytes or None on any failure.
    /// MUST be called from a spawned thread — GDI calls take 5-50ms per exe.
    pub(crate) fn extract_icon_png(exe_path: &Path) -> Option<Vec<u8>> {
        todo!("Phase 3 Plan 03: implement icon extraction")
    }

    /// Try to start an index run. No-op if already indexing (AtomicBool guard).
    /// Spawns a thread, sets flag true, runs index, sets flag false on completion.
    pub(crate) fn try_start_index(
        is_indexing: &Arc<AtomicBool>,
        db: &Arc<Mutex<Connection>>,
        data_dir: &Path,
        settings: &Settings,
    ) {
        todo!("Phase 3 Plan 05: implement try_start_index")
    }

    // ---- Tests ----

    #[cfg(test)]
    mod tests {
        use super::*;
        use rusqlite::Connection;
        use std::fs;
        use tempfile::tempdir; // NOTE: tempfile is a dev-dependency — add to Cargo.toml if not present

        // Test helpers
        fn in_memory_db() -> Connection {
            let conn = Connection::open_in_memory().unwrap();
            crate::db::init_db_connection(&conn).unwrap();
            conn
        }

        fn temp_dir_with_exe(name: &str) -> (tempfile::TempDir, PathBuf) {
            let dir = tempdir().unwrap();
            let exe_path = dir.path().join(name);
            fs::write(&exe_path, b"MZ").unwrap(); // minimal DOS header stub
            (dir, exe_path)
        }

        #[test]
        #[should_panic(expected = "todo")]
        fn test_crawl_discovers_exe() {
            let (dir, _exe) = temp_dir_with_exe("foo.exe");
            let apps = crawl_dir(dir.path(), "additional", &[]);
            assert_eq!(apps.len(), 1);
            assert!(apps[0].path.ends_with("foo.exe"));
        }

        #[test]
        #[ignore] // .lnk creation requires Windows shell APIs — tested manually in smoke test
        fn test_crawl_discovers_lnk() {
            // Will be enabled when a .lnk test fixture is available
        }

        #[test]
        #[ignore] // Requires a real .lnk file on disk
        fn test_resolve_lnk_valid() {
            // Verified in smoke test: Start Menu .lnk → exe path
        }

        #[test]
        #[should_panic(expected = "todo")]
        fn test_resolve_lnk_broken() {
            let result = resolve_lnk(Path::new("C:\\nonexistent\\fake.lnk"));
            assert!(result.is_none());
        }

        #[test]
        #[should_panic(expected = "todo")]
        fn test_prune_stale() {
            let conn = in_memory_db();
            // Insert two apps
            let app1 = AppRecord {
                id: "app1".to_string(), name: "App 1".to_string(),
                path: "C:\\app1.exe".to_string(), icon_path: None,
                source: "start_menu".to_string(), last_launched: None, launch_count: 0,
            };
            let app2 = AppRecord {
                id: "app2".to_string(), name: "App 2".to_string(),
                path: "C:\\app2.exe".to_string(), icon_path: None,
                source: "start_menu".to_string(), last_launched: None, launch_count: 0,
            };
            crate::db::upsert_app(&conn, &app1).unwrap();
            crate::db::upsert_app(&conn, &app2).unwrap();
            // Only app1 was discovered this index
            let mut discovered = HashSet::new();
            discovered.insert("app1".to_string());
            prune_stale(&conn, &discovered).unwrap();
            let remaining = crate::db::get_all_apps(&conn).unwrap();
            assert_eq!(remaining.len(), 1);
            assert_eq!(remaining[0].id, "app1");
        }

        #[test]
        #[should_panic(expected = "todo")]
        fn test_crawl_excludes_path() {
            let dir = tempdir().unwrap();
            let excluded_sub = dir.path().join("excluded");
            fs::create_dir_all(&excluded_sub).unwrap();
            fs::write(excluded_sub.join("hidden.exe"), b"MZ").unwrap();
            fs::write(dir.path().join("visible.exe"), b"MZ").unwrap();
            let excluded = vec![excluded_sub.to_string_lossy().to_string()];
            let apps = crawl_dir(dir.path(), "additional", &excluded);
            // Only visible.exe should appear
            assert_eq!(apps.len(), 1);
            assert!(apps[0].path.contains("visible.exe"));
        }

        #[test]
        #[should_panic(expected = "todo")]
        fn test_icon_filename_stable() {
            let path = "C:\\Windows\\notepad.exe";
            let a = icon_filename(path);
            let b = icon_filename(path);
            assert_eq!(a, b);
            assert!(a.ends_with(".png"));
            assert_eq!(a.len(), 20); // 16 hex chars + ".png"
        }

        #[test]
        #[should_panic(expected = "todo")]
        fn test_generic_icon_bootstrap() {
            let dir = tempdir().unwrap();
            ensure_generic_icon(dir.path()).unwrap();
            let generic_path = dir.path().join("icons").join("generic.png");
            assert!(generic_path.exists());
            let size1 = fs::metadata(&generic_path).unwrap().len();
            // Second call is no-op
            ensure_generic_icon(dir.path()).unwrap();
            let size2 = fs::metadata(&generic_path).unwrap().len();
            assert_eq!(size1, size2);
        }

        #[test]
        #[should_panic(expected = "todo")]
        fn test_timer_fires() {
            use std::sync::atomic::AtomicUsize;
            use std::time::Duration;
            let counter = Arc::new(AtomicUsize::new(0));
            let counter_clone = Arc::clone(&counter);
            // This test will be properly implemented in Plan 05
            // For now: verify start_background_tasks signature compiles
            let _ = counter_clone;
        }

        #[test]
        #[should_panic(expected = "todo")]
        fn test_timer_reset() {
            // Timer reset signal via mpsc::Sender<()>
            // Will be implemented in Plan 05
        }

        #[test]
        #[should_panic(expected = "todo")]
        fn test_atomic_guard_prevents_double_index() {
            let flag = Arc::new(AtomicBool::new(false));
            let db = Arc::new(Mutex::new(Connection::open_in_memory().unwrap()));
            let settings = Settings::default();
            let dir = tempdir().unwrap();
            // First call should start; second concurrent call should be dropped
            try_start_index(&flag, &db, dir.path(), &settings);
            try_start_index(&flag, &db, dir.path(), &settings);
        }
    }
}
```

    IMPORTANT notes for the executor:
    1. The `tempfile` crate may not be in Cargo.toml yet. Check — if missing, add `tempfile = "3"` under `[dev-dependencies]` in Cargo.toml.
    2. The test module closing brace at the end has an extra `}` in the scaffold above — the outer module closes the `mod tests` block and the file. Remove the extra `}` if it causes a compile error.
    3. All tests use `#[should_panic(expected = "todo")]` which means they are RED (failing as todo! panics) until Plans 02-05 implement the actual functions. This is correct Nyquist behavior.
    4. The `#[ignore]` tests for LNK do NOT fail — they are skipped. This is intentional.
    5. After writing indexer.rs, run `cargo test -p riftle --lib indexer` and confirm tests FAIL with "todo" panics (not compile errors).
  </action>
  <verify>
    <automated>cd /d/develop/projects/riftle/.claude/worktrees/eloquent-clarke/src-tauri && cargo test -p riftle --lib indexer 2>&1 | grep -E "(test result|FAILED|error\[)" | head -20</automated>
  </verify>
  <done>
    - src-tauri/icons/generic.png exists as a valid PNG file (non-zero bytes)
    - cargo test -p riftle --lib indexer compiles successfully
    - Tests that use #[should_panic(expected = "todo")] show as FAILED (not compile errors) — this is correct RED state
    - #[ignore] tests are skipped (shown as "ignored")
    - No compile errors ("error[" lines) in output
  </done>
</task>

</tasks>

<verification>
Run the full library test suite to confirm Wave 0 is stable:

```bash
cd /d/develop/projects/riftle/.claude/worktrees/eloquent-clarke/src-tauri && cargo test -p riftle --lib 2>&1 | tail -20
```

Expected: All existing Phase 2 tests pass. Indexer tests fail with "todo" panics (RED state — correct). No compile errors.
</verification>

<success_criteria>
1. Cargo.toml has lnk ^0.3, notify-debouncer-mini 0.4, image ^0.25 (png only), Win32_Graphics_Gdi, Win32_UI_WindowsAndMessaging
2. src-tauri/icons/generic.png is a valid non-empty PNG file
3. indexer.rs has all public function signatures: run_full_index, start_background_tasks, reindex, get_index_paths, crawl_dir, resolve_lnk, make_app_record, icon_filename, prune_stale, ensure_generic_icon, extract_icon_png, try_start_index
4. All 10 non-ignored test stubs compile and fail with "todo" panics
5. cargo test -p riftle --lib passes for Phase 2 modules (db, store, paths)
</success_criteria>

<output>
After completion, create `.planning/phases/03-indexer/03-01-SUMMARY.md`
</output>
