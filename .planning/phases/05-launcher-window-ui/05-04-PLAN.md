---
phase: 05-launcher-window-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src-tauri/src/search.rs
  - src/App.vue
autonomous: true
gap_closure: true
requirements:
  - LWND-09

must_haves:
  truths:
    - "Admin badge appears on every result row that requires elevation, consistently, without any keyboard interaction"
    - "Admin badge is visible by default on applicable rows, not contingent on Ctrl+Shift being held"
  artifacts:
    - path: "src-tauri/src/search.rs"
      provides: "SearchResult struct with requires_elevation: bool field"
      contains: "requires_elevation"
    - path: "src/App.vue"
      provides: "TypeScript SearchResult interface mirrors requires_elevation; badge v-if uses item.requires_elevation"
      contains: "requires_elevation"
  key_links:
    - from: "src-tauri/src/search.rs SearchResult.requires_elevation"
      to: "src/App.vue badge v-if"
      via: "Tauri invoke('search') serializes requires_elevation into JSON payload consumed by Vue template"
      pattern: "requires_elevation"
---

<objective>
Fix the admin badge visibility bug: badge currently only shows while Ctrl+Shift is held on the selected row. After this fix the badge appears on every result row that has requires_elevation: true, independent of keyboard state.

Purpose: The UAT confirmed this as a major gap (Test 8). The root cause is that the badge v-if is gated on the transient adminMode ref (keyboard state) instead of a stable per-result flag. Decoupling the badge from adminMode requires adding requires_elevation: bool to the SearchResult Rust struct (so results carry their own elevation status) and updating the Vue template v-if accordingly.

Output: requires_elevation field on SearchResult in Rust + TypeScript; badge v-if changed to item.requires_elevation.
</objective>

<execution_context>
@C:/Users/Balazs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Balazs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-launcher-window-ui/05-02-SUMMARY.md
@.planning/phases/05-launcher-window-ui/05-03-SUMMARY.md

<interfaces>
<!-- Current SearchResult in src-tauri/src/search.rs (lines 19-26) -->
```rust
#[derive(Debug, Clone, Serialize)]
pub struct SearchResult {
    pub id: String,
    pub name: String,
    pub icon_path: String,
    pub path: String,
    pub kind: String,
}
```

<!-- Current TypeScript SearchResult in src/App.vue (lines 10-16) -->
```typescript
interface SearchResult {
  id: string
  name: string
  icon_path: string
  path: string
  kind: string
}
```

<!-- Current badge template in src/App.vue (line 307) — THE BUG -->
```html
<!-- Admin badge (right margin, no layout shift) -->
<span
  v-if="index === selectedIndex && adminMode"
  class="admin-badge"
  aria-label="Elevate with admin rights"
>[Admin]</span>
```

<!-- adminMode is a transient ref set on keydown/keyup -->
<!-- adminMode.value = e.ctrlKey && e.shiftKey  (line 112) -->
<!-- adminMode.value = e.ctrlKey && e.shiftKey  (line 146, keyup) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requires_elevation field to SearchResult Rust struct</name>
  <files>src-tauri/src/search.rs</files>
  <action>
Add `requires_elevation: bool` to the `SearchResult` struct (after the `kind` field). This field must be populated in every place SearchResult values are constructed.

In `src-tauri/src/search.rs`:

1. Add the field to the struct:

```rust
#[derive(Debug, Clone, Serialize)]
pub struct SearchResult {
    pub id: String,
    pub name: String,
    pub icon_path: String,
    pub path: String,
    pub kind: String,
    pub requires_elevation: bool,
}
```

2. Find every location in search.rs where a SearchResult is constructed (struct literal initialisation) and add `requires_elevation: false` to app results and `requires_elevation: false` to system command results. System commands (lock, shutdown, restart, sleep) do not require elevation from the badge perspective — they use a different path (run_system_command). App results default to false; Phase 6 or 8 can wire real elevation detection later.

Search the file for `SearchResult {` to find all construction sites. The pattern will be in `score_and_rank` and `search_system_commands` functions (or wherever results are built). Set `requires_elevation: false` for all of them.

Do NOT change any test assertions unless they fail to compile — the new field has a default value and any existing struct literal construction will need the new field added.

Verify with: `cd /d/develop/projects/riftle/.claude/worktrees/vigilant-mahavira && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -20`
  </action>
  <verify>
    <automated>cd /d/develop/projects/riftle/.claude/worktrees/vigilant-mahavira && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -20</automated>
  </verify>
  <done>search.rs SearchResult struct has requires_elevation: bool field; cargo build succeeds with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Mirror requires_elevation in TypeScript interface and fix badge v-if</name>
  <files>src/App.vue</files>
  <action>
Two changes to src/App.vue:

**Change 1 — TypeScript interface** (around line 10-16):

Add `requires_elevation: boolean` to the SearchResult interface:

```typescript
interface SearchResult {
  id: string
  name: string
  icon_path: string
  path: string
  kind: string
  requires_elevation: boolean
}
```

**Change 2 — Badge v-if** (around line 306-311):

The current badge uses `v-if="index === selectedIndex && adminMode"`. Replace with `v-if="item.requires_elevation"`.

The badge should appear on ALL rows where the app requires elevation — not just the selected row and not gated on keyboard state. The `adminMode` ref and `onKeyUp` handler remain in place because Ctrl+Shift+Enter still triggers elevated launch — adminMode is used in the Enter key handler (`if (e.ctrlKey && e.shiftKey) { launchElevated(item) }`). Do NOT remove adminMode or the keyboard handlers; only change the badge v-if.

Final badge markup:

```html
<!-- Admin badge (right margin, no layout shift) -->
<span
  v-if="item.requires_elevation"
  class="admin-badge"
  aria-label="Elevate with admin rights"
>[Admin]</span>
```

After making these changes, run `pnpm build` to confirm no TypeScript errors.
  </action>
  <verify>
    <automated>cd /d/develop/projects/riftle/.claude/worktrees/vigilant-mahavira && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript interface has requires_elevation: boolean; badge v-if is item.requires_elevation; pnpm build succeeds</done>
</task>

</tasks>

<verification>
Gap closed when:
- cargo build succeeds (Rust struct updated)
- pnpm build succeeds (TypeScript interface updated, no type errors)
- Badge v-if is `item.requires_elevation` (grep confirms: `grep "requires_elevation" src/App.vue`)
- No badge v-if references adminMode (grep confirms absence: `grep "adminMode" src/App.vue | grep "v-if"` returns only keyboard handler lines, not the badge span)
</verification>

<success_criteria>
- SearchResult Rust struct has requires_elevation: bool
- TypeScript SearchResult interface has requires_elevation: boolean
- Badge span v-if="item.requires_elevation" (not gated on adminMode or selectedIndex)
- All existing tests pass (cargo test in src-tauri)
- pnpm build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-launcher-window-ui/05-04-SUMMARY.md` using the summary template.
</output>
